---
quick_task: 260909-2rd
plan: 01
type: execute
autonomous: true
files_modified:
  - web/src/styles/accounts.css
must_haves:
  truths:
    - "At 390px, the Accounts hero ends about 45–55px after the benefit grid instead of leaving the current 179px dead gap."
    - "Mobile eyebrow, heading, body copy, benefit icons, and benefit labels are larger and retain a clear, compact vertical rhythm."
    - "The hero and first account card remain unclipped and free of horizontal overflow at both 390px and 320px."
    - "Desktop and tablet presentation, Accounts content, and account cards remain unchanged."
  artifacts:
    - path: "web/src/styles/accounts.css"
      provides: "Content-sized mobile Accounts hero and mobile-only typography/benefit scaling"
  key_links:
    - from: "web/src/styles/accounts.css"
      to: ".accounts-hero"
      via: "mobile media-query overrides remove the fixed minimum height"
      pattern: "@media \\(max-width: 767px\\)"
    - from: "web/src/styles/accounts.css"
      to: ".accounts-hero__benefits"
      via: "mobile icon, label, grid-gap, and spacing rules"
      pattern: "accounts-hero__benefits"
---

<objective>
Refine the Accounts page mobile hero so its content uses the viewport efficiently, with stronger typography and benefits followed by roughly 45–55px of breathing room rather than a large empty tail.

Purpose: Improve mobile hierarchy and density without changing desktop/tablet layout, account cards, content, or component markup.
Output: A single responsive CSS refinement in `web/src/styles/accounts.css`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.cursor/skills/dominion-section-redesign/SKILL.md
@web/src/styles/accounts.css
@web/src/components/accounts/AccountsPage.tsx
@/Users/usama/.cursor/projects/Users-usama-Desktop-Dominion-Markets-Fresh-Pull/assets/image-e1594a78-2a7a-4e16-bd54-cd8cd785c13f.png

<baseline>
Measured at 390px: hero 590px tall; heading 166–215px; body 230–301px; benefits 334–411px; 179px dead gap below benefits; first card starts at 618px.
</baseline>

<locked_decisions>
- D-01: Modify only `web/src/styles/accounts.css`.
- D-02: Combine mobile scale increases with removal of the unnecessary mobile hero minimum height; content must determine height with about 45–55px bottom breathing room.
- D-03: Scale the mobile eyebrow, headline, body, benefit icons, and labels while improving vertical rhythm.
- D-04: Preserve desktop/tablet layout and do not alter account-card styling or content.
- D-05: Verify 390px geometry/screenshots and 320px horizontal-overflow safety.
- D-06: Do not touch `.DS_Store` and do not push. If committing, commit the source stylesheet only; parent handles planning documents.
</locked_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebalance the mobile Accounts hero density</name>
  <files>web/src/styles/accounts.css</files>
  <action>
Edit only hero-related declarations inside `@media (max-width: 767px)` and `@media (max-width: 420px)` per D-01/D-04. Leave base, desktop/tablet, flow, card, comparison, and later-section rules unchanged.

Replace the mobile `.accounts-hero` minimum-height constraint with content sizing (`min-height: 0`) and remove the 590px override at `max-width: 420px`. Tune mobile hero bottom padding so the measured distance from the benefit grid bottom to the hero bottom is 45–55px at 390px; retain the existing navigation-safe top offset unless browser measurement shows a small rhythm correction is required.

Per D-02/D-03, increase the mobile hierarchy without merely stretching the section: use a fluid headline scale that is visibly stronger around 390px but still fits at 320px; increase eyebrow and body-copy sizes/line heights; enlarge benefit icons through scoped width/height rules and increase benefit-label size/line height. Rebalance breadcrumb-to-copy, paragraph-to-benefits, and benefit row/column gaps so the composition remains compact and deliberate. Keep the two-column benefit grid, allow long labels to wrap, and do not use fixed hero height, absolute positioning, transforms, or negative margins to manufacture the target geometry.

Run lint and TypeScript checks. Then launch `/accounts` and inspect it with browser tooling at exactly 390px and 320px. At 390px, record hero and benefit-grid bounding rectangles and confirm a 45–55px bottom gap; capture a temporary screenshot showing the hero through the first card. At 320px, verify `document.documentElement.scrollWidth <= document.documentElement.clientWidth`, inspect headline/benefit wrapping and clipping, and capture a temporary screenshot. Also spot-check a desktop width (at least 1280px) to confirm the media-only change preserved desktop. Do not commit screenshots.

Per D-06, leave `.DS_Store` untouched and do not push. If the executor commits, stage and commit only `web/src/styles/accounts.css`; do not commit this plan or generated summary because the parent owns planning artifacts.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run lint -- src/styles/accounts.css &amp;&amp; npx tsc --noEmit</automated>
    <manual>Using browser geometry at `/accounts`: at 390px confirm `heroBottom - benefitsBottom` is 45–55px and save a temporary hero-through-first-card screenshot; at 320px confirm `scrollWidth &lt;= clientWidth`, no clipping, and save a temporary screenshot; at 1280px confirm the hero remains visually unchanged.</manual>
  </verify>
  <done>`accounts.css` alone delivers a content-height mobile hero, larger readable hero/benefit elements, a measured 45–55px lower breathing space at 390px, no horizontal overflow at 320px, and no desktop/tablet or account-card regression.</done>
</task>

</tasks>

<threat_model>
- T-260909-2rd-01 (Denial of service/layout): Enlarged mobile text or icons could create horizontal overflow at 320px. Mitigate with fluid sizing, wrapping, scoped selectors, and an explicit browser `scrollWidth` check.
- T-260909-2rd-02 (Tampering/regression): Broad selectors could alter desktop or account cards. Mitigate by changing only hero selectors in the existing mobile media queries and spot-checking at 1280px.
- No user input, data, authentication, or external-service trust boundary changes are introduced by this CSS-only task.
</threat_model>

<verification>
1. Run `cd web && npm run lint -- src/styles/accounts.css && npx tsc --noEmit`.
2. Measure `/accounts` at 390px and confirm 45–55px from benefit-grid bottom to hero bottom; capture a temporary screenshot through the first card.
3. Inspect `/accounts` at 320px for wrapping/clipping and confirm no horizontal document overflow; capture a temporary screenshot.
4. Spot-check `/accounts` at 1280px for unchanged desktop presentation.
5. Confirm only `web/src/styles/accounts.css` is a source change, `.DS_Store` is untouched, screenshots are uncommitted, and nothing is pushed.
</verification>

<success_criteria>
- Mobile hero height is content-driven rather than held at 570/590px.
- The 390px bottom gap measures 45–55px instead of 179px.
- Mobile eyebrow, headline, body copy, benefit icons, and labels are larger with balanced spacing.
- The 320px layout has no horizontal overflow or clipped hero content.
- Desktop/tablet and account cards remain unchanged.
- Lint and TypeScript checks pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260909-2rd-improve-accounts-page-mobile-hero-densit/260909-2rd-SUMMARY.md`; keep it out of any source-only executor commit because the parent handles planning artifacts.
</output>
