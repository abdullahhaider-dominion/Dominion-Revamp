---
quick_task: 260909-26l
plan: 01
type: execute
autonomous: true
files_modified:
  - web/src/app/accounts/page.tsx
  - web/src/components/accounts/AccountsPage.tsx
  - web/src/styles/accounts.css
  - web/src/content/site-nav.ts
  - web/src/components/SiteNav.tsx
  - web/public/assets/Accounts-page/accounts-mist-portrait.png
  - web/public/assets/Accounts-page/accounts-hero-mountains.png
  - web/public/assets/Accounts-page/accounts-cta-mountains.png
must_haves:
  truths:
    - "A visitor can open /accounts and move through the complete Accounts page from hero and account cards to comparison, benefits, three-step onboarding, FAQ, final CTA, and the shared footer."
    - "The page closely matches the supplied desktop reference while remaining readable, navigable, and free of horizontal overflow on mobile."
    - "All three supplied mountain backgrounds and all seventeen supplied Accounts-page icons appear in purposeful roles."
    - "Account spreads, one-way commissions, minimum deposits, platforms, and positioning match the legible reference and approved project spec."
    - "The existing SiteNav, SiteFooter, Poppins headings, and Montserrat body typography are reused without duplication or new font dependencies."
    - "Primary Accounts navigation and account-opening entry points resolve to /accounts."
  artifacts:
    - path: "web/src/app/accounts/page.tsx"
      provides: "Dedicated /accounts App Router entry point and page metadata"
    - path: "web/src/components/accounts/AccountsPage.tsx"
      provides: "Typed, semantic Accounts page content and reusable mapped sections"
    - path: "web/src/styles/accounts.css"
      provides: "Reference-matched desktop presentation and responsive mobile/table behavior"
    - path: "web/src/content/site-nav.ts"
      provides: "Accounts navigation destination"
    - path: "web/public/assets/Accounts-page/"
      provides: "Three mountain atmospheres and seventeen purpose-mapped icons"
  key_links:
    - from: "web/src/app/accounts/page.tsx"
      to: "web/src/components/accounts/AccountsPage.tsx"
      via: "AccountsPage import and render"
      pattern: "import.*AccountsPage"
    - from: "web/src/components/accounts/AccountsPage.tsx"
      to: "web/src/styles/accounts.css"
      via: "route component stylesheet import"
      pattern: "styles/accounts\\.css"
    - from: "web/src/components/accounts/AccountsPage.tsx"
      to: "web/public/assets/Accounts-page/"
      via: "local Next Image and icon sources"
      pattern: "/assets/Accounts-page/"
    - from: "web/src/components/SiteNav.tsx"
      to: "/accounts"
      via: "desktop and mobile Get Started links"
      pattern: "href=\"/accounts\""
---

<objective>
Build the production Accounts page shown in the supplied reference, using the supplied Accounts-page imagery and the site's established App Router, shared chrome, typography, and brand system.

Purpose: Give visitors a complete, trustworthy account-selection journey at `/accounts` without introducing a parallel design system or duplicating global navigation/footer code.
Output: A typed Accounts route and component, dedicated responsive stylesheet, semantically named mountain assets, and corrected Accounts navigation links.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.cursor/skills/dominion-section-redesign/SKILL.md
@docs/brand/Dominion_Markets_Brand_Guidelines.md
@docs/superpowers/specs/2026-08-07-dominion-markets-floating-sanctuary-design.md
@web/src/app/layout.tsx
@web/src/app/globals.css
@web/src/components/SiteNav.tsx
@web/src/components/SiteFooter.tsx
@web/src/content/site-nav.ts
@web/src/app/incentives/copy-trading/page.tsx
@web/src/components/copy-trading/CopyTrading.tsx
@web/src/styles/copy-trading.css
@/Users/usama/.cursor/projects/Users-usama-Desktop-Dominion-Markets-Fresh-Pull/assets/image-0a582098-dc60-4307-82b6-aea614c7c312.jpg

<locked_decisions>
- D-01: Recreate the supplied reference closely; do not design alternate concepts.
- D-02: Use all supplied Accounts-page assets: three mountain backgrounds and all seventeen icons.
- D-03: Reuse the global Poppins/Montserrat typography, SiteNav, and SiteFooter.
- D-04: Deliver deliberate desktop and mobile layouts.
- D-05: Do not modify, stage, or commit the unrelated root `.DS_Store`.
- D-06: Do not push.
</locked_decisions>

<interfaces>
From `web/src/app/layout.tsx`:
```typescript
<body className={montserrat.className}>
  <SiteNav />
  {children}
  <SiteFooter />
</body>
```
The route must render only its `<main>` content; shared chrome already comes from the root layout.

From `web/src/content/site-nav.ts`:
```typescript
export type SiteNavItem = SiteNavLink & {
  children?: readonly SiteNavLink[];
};

export const SITE_NAV: readonly SiteNavItem[] = [/* ... */];
```

From `web/src/app/globals.css`:
```css
:root {
  --dm-font-body: var(--font-montserrat), "Montserrat", system-ui, sans-serif;
  --dm-font-heading: var(--font-poppins), "Poppins", system-ui, sans-serif;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the typed Accounts route and full semantic content</name>
  <files>web/src/app/accounts/page.tsx, web/src/components/accounts/AccountsPage.tsx, web/src/styles/accounts.css, web/public/assets/Accounts-page/accounts-mist-portrait.png, web/public/assets/Accounts-page/accounts-hero-mountains.png, web/public/assets/Accounts-page/accounts-cta-mountains.png</files>
  <action>
Create the thin `/accounts` App Router page with Accounts-specific title, description, and canonical metadata, then render a named `AccountsPage` component. Do not import or duplicate `SiteNav` or `SiteFooter`; root layout already supplies both, per D-03.

Rename only the three generically named supplied backgrounds so production references are stable:
- `ChatGPT Image Sep 9, 2026, 01_28_58 AM.png` → `accounts-mist-portrait.png`
- `ChatGPT Image Sep 9, 2026, 01_29_05 AM.png` → `accounts-hero-mountains.png`
- `ChatGPT Image Sep 9, 2026, 01_29_12 AM.png` → `accounts-cta-mountains.png`

In `AccountsPage.tsx`, define explicit local TypeScript contracts and readonly arrays for account types, comparison features, benefit cards, onboarding steps, and FAQs. Render from those arrays rather than repeating hardcoded card/table/FAQ markup. Use semantic `<main>`, labelled `<section>`, `<article>`, `<table>` with scoped row/column headers, `<dl>` for the mobile comparison representation, `<ol>` for steps, and native `<details>/<summary>` for keyboard-accessible FAQs. Use `next/image` for the hero and final CTA backgrounds; keep decorative imagery/icons at empty alt text and meaningful labels in visible text.

Implement all reference sections in order: hero/breadcrumb and benefit strip; four account cards; “Which Account Fits Your Trading Style?” comparison; six “Why Trade With Dominion Markets?” benefits; Register/Fund/Trade steps; six FAQs; and final registration CTA. The footer must follow automatically from the root layout.

Use the legible reference plus approved spec as the data authority. The comparison data must be:
- Standard STP: novice traders; spread from 2.0 pip; $0.00 one-way commission; $50 minimum; cTrader; simple and accessible.
- ECN: experienced traders; spread from 0.0 pip; $3.5 one-way commission; $100 minimum; cTrader; deep liquidity and tight spreads.
- ECN Institutional: professional traders; spread from 0.0 pip; $3.0 one-way commission; $20,000 minimum; MetaTrader 5; institutional-grade liquidity.
- Islamic: traders seeking Sharia-compliant conditions; spread from 1.0 pip; $3.5 one-way commission; $50 minimum; cTrader; swap-free trading.

Keep supporting card bullets consistent with those facts and the approved spec: Standard STP has competitive spreads/no commissions; ECN has raw spreads plus transparent commission; ECN Institutional is MT5-only with institutional liquidity; Islamic is swap-free. Do not introduce unsupported performance, profitability, or “risk-free” claims. Derive FAQ answers from the same typed account data so copy cannot contradict the cards/table.

Per D-02, purpose-map every supplied icon: `layers`, `infinity`, `shield-check`, and `users` in the hero benefit strip; `layers`, `network`, `institution`, and `crescent-star` on account cards; `check-circle` for included features; `bar-chart`, `lightning`, `platforms`, `shield-lock`, `headset`, and `file-shield` for the six benefits; and `document-edit`, `wallet`, and `trend-up` for the three steps. Create the stylesheet file and import it, with only enough foundational rules in this task for the component to compile; visual completion is Task 2.

Use existing site-local CTA conventions rather than inventing an external registration URL: account-opening/register links target `/#final-cta`, and client login targets `/#client-area`. Links must never use an empty or bare `#` href.

Do not touch or stage `.DS_Store` per D-05, and do not push per D-06.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run lint -- src/app/accounts/page.tsx src/components/accounts/AccountsPage.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>`/accounts` compiles, exposes route-specific metadata, contains every requested section in semantic order, generates repeated UI from typed arrays, uses all seventeen supplied icons, and presents one internally consistent account-data matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Match the reference and implement responsive layouts</name>
  <files>web/src/styles/accounts.css</files>
  <action>
Complete a route-scoped `.accounts-page` stylesheet that closely matches the supplied reference per D-01: pale blue/green mountain mist atmosphere, navy Poppins headings, compact Montserrat copy, translucent milky panels, Market Green icons/CTAs, four aligned desktop account cards, dense comparison treatment, six compact benefit modules, three numbered steps, two-column FAQ rows, and the closing CTA. Use exact brand colors `#1D3347` and `#47BD68`, global font variables, and the existing rounded/glass language; add no font import or dependency per D-03.

Use all three renamed backgrounds per D-02: the landscape mountain art in the hero, the portrait mist art as the long light-page atmosphere behind the account/comparison/benefit/step/FAQ flow, and the dark mountain/chart art in the final CTA with a controlled navy veil for text contrast. Apply an Accounts-route nav treatment through `body:has(.accounts-page) .dm-nav` that preserves the existing reverse logo and shared nav behavior while remaining readable over the light hero.

Implement desktop, tablet, and mobile compositions per D-04:
- Desktop: four account cards in one row; six benefits in one row where space permits; three steps in one row; two FAQ columns; constrained content width matching the reference.
- Tablet: account cards 2-up, benefits 3-up, and comfortable comparison sizing.
- Mobile (around 390px): single-column account cards/steps/FAQs, two-up or single-column benefits without clipped copy, full-width CTAs with at least 44px touch height, and compact heading/type scales.
- Keep the semantic comparison table for desktop/tablet and show account-first `<dl>` comparison cards generated from the same data on narrow screens; hide only the alternate presentation at the relevant breakpoint so assistive technology receives one active representation.

Add visible `:focus-visible` states for links and FAQ summaries, hover effects that do not carry essential meaning, and `prefers-reduced-motion` handling. Prevent horizontal page overflow, preserve readable contrast, and avoid shrinking the desktop table into illegible text.

Run browser inspection against the local `/accounts` route at 1440px desktop and 390px mobile. Confirm the complete page and shared footer render, all image requests succeed, no section clips or overlaps, the comparison changes strategy at mobile, FAQ summaries open by mouse and keyboard, nav drawer remains usable, and CTAs have valid destinations. Capture temporary desktop/mobile screenshots for comparison with the supplied reference, but do not commit test screenshots.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run lint -- src/app/accounts/page.tsx src/components/accounts/AccountsPage.tsx &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
    <manual>Compare `/accounts` at 1440px and 390px against the supplied reference; verify no horizontal overflow, successful local assets, keyboard-operable FAQs, responsive comparison presentation, nav usability, and shared footer continuity.</manual>
  </verify>
  <done>The Accounts page is a close visual recreation on desktop, has a purpose-built mobile composition rather than a scaled-down desktop layout, uses all three backgrounds, and passes interaction/accessibility browser checks.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget Accounts navigation and run release gates</name>
  <files>web/src/content/site-nav.ts, web/src/components/SiteNav.tsx</files>
  <action>
Update the shared navigation per D-03: change the top-level `Accounts` href and its `Account types` child href from `/#accounts` to `/accounts`; retain the `Funding` child at its existing homepage funding anchor because this page does not add a funding section. Change both desktop and mobile SiteNav “Get Started” links from `/#accounts` to `/accounts`. Do not broaden scope to unrelated account CTAs in incentive pages.

Run final static and production build gates, then repeat browser smoke checks for desktop and mobile route entry, nav-to-Accounts behavior, dropdown/drawer behavior, FAQ keyboard interaction, CTA links, and footer placement. Inspect the browser network/console for missing assets or React errors. Confirm each supplied Accounts-page icon filename and each renamed background is referenced by production source.

Stage/commit only the plan-listed Accounts implementation, renamed Accounts assets, and nav files when the quick-task executor commits. Leave the unrelated root `.DS_Store` untracked and unchanged per D-05. Do not push per D-06.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; cd .. &amp;&amp; test -z "$(git diff --name-only -- .DS_Store)" &amp;&amp; ! git ls-files --error-unmatch .DS_Store &gt;/dev/null 2&gt;&amp;1</automated>
    <manual>At desktop and mobile widths, activate the Accounts nav item and Get Started CTA, verify both reach `/accounts`, open each FAQ by keyboard, and confirm the shared footer follows the final CTA without duplication.</manual>
  </verify>
  <done>All requested navigation entry points reach `/accounts`; lint, typecheck, and production build pass; browser checks pass at desktop and mobile; all supplied assets are referenced; `.DS_Store` remains untouched; and nothing is pushed.</done>
</task>

</tasks>

<threat_model>
## Trust boundaries

- Static React content → browser: no user data is accepted; all account content comes from typed local constants.
- Browser → CTA destinations: navigation must use explicit internal paths and must not create deceptive or empty links.
- Public assets → rendered page: only repository-local supplied image files are used; no remote third-party media is introduced.

## STRIDE dispositions

- T-260909-26l-01 (Tampering): Divergent duplicated account facts could misstate trading conditions. Mitigate by rendering cards, desktop comparison, mobile comparison, and FAQs from one typed account dataset.
- T-260909-26l-02 (Spoofing): Ambiguous or empty CTA destinations could misrepresent registration/login actions. Mitigate with explicit existing internal destinations and semantic `Link` elements.
- T-260909-26l-03 (Information disclosure): The page has no forms, credentials, personal data, API calls, or runtime secrets. Accept; no sensitive boundary exists in scope.
- T-260909-26l-04 (Denial of service): Large decorative images could cause avoidable page-load pressure. Mitigate with `next/image`, correct `sizes`, hero-only priority, and non-priority loading below the fold.
- T-260909-26l-05 (Elevation of privilege): No authentication, authorization, mutation, or executable user input exists. Accept as not applicable to this static route.
</threat_model>

<verification>
1. Run `cd web && npm run lint && npx tsc --noEmit && npm run build`.
2. Run the production or development server and inspect `/accounts` at 1440px and 390px.
3. Confirm route/nav behavior, successful asset requests, no console errors, no horizontal overflow, keyboard-operable FAQ items, mobile comparison cards, and shared footer continuity.
4. Confirm production source references all seventeen icon files and all three renamed mountain backgrounds.
5. Confirm root `.DS_Store` is still untracked/unchanged and do not push.
</verification>

<success_criteria>
- `/accounts` is a production App Router route with route-specific metadata.
- The rendered section order and visual hierarchy closely match the supplied reference.
- The four account types use the approved spreads, commissions, deposits, platforms, and positioning.
- All supplied Accounts-page assets have purposeful production references.
- Desktop and mobile layouts pass browser checks without clipping or horizontal overflow.
- FAQ interactions and CTAs are keyboard accessible and have valid destinations.
- Existing global fonts, SiteNav, and SiteFooter are reused.
- Full lint, TypeScript, and Next.js build gates pass.
- Root `.DS_Store` is neither modified nor committed, and no push occurs.
</success_criteria>

<output>
After completion, create `.planning/quick/260909-26l-build-a-production-accounts-page-matchin/260909-26l-SUMMARY.md`.
</output>
