---
phase: 06-parity-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-parity-verification/scripts/parity-ref-check.mjs
  - .planning/phases/06-parity-verification/06-PARITY-CHECKLIST.md
  - .planning/phases/06-parity-verification/06-01-SUMMARY.md
  - web/src/content/home-html.ts
  - web/src/components/CinematicHero.tsx
  - web/src/app/layout.tsx
  - web/public/mockups.css
autonomous: true
requirements:
  - PROD-01
user_setup: []

must_haves:
  truths:
    - "Path-aware ref-check reports 0 missing referenced web/public paths and 0 references to 05-DELETE-LIST.txt paths"
    - "All 55 delete-list paths remain absent from disk; all 02-INVENTORY.json class=keep paths remain present"
    - "cd web && npm run build exits 0"
    - "06-PARITY-CHECKLIST.md exists covering hero/nav, proof→final-cta (+ footer), and key widgets; automated rows marked PASS from script/build"
    - "Any code change is D-04 minimal fix for prune-caused breaks only (or no web/ code change if asserts already green)"
  artifacts:
    - path: ".planning/phases/06-parity-verification/scripts/parity-ref-check.mjs"
      provides: "Reproducible path-aware Assert A/B/C gate (Nyquist Wave 0)"
      min_lines: 80
      contains: "05-DELETE-LIST"
    - path: ".planning/phases/06-parity-verification/06-PARITY-CHECKLIST.md"
      provides: "Hybrid UAT: auto + human_needed rows for PROD-01"
      min_lines: 40
      contains: "final-cta"
    - path: ".planning/phases/06-parity-verification/06-01-SUMMARY.md"
      provides: "Execution proof with PROD-01 evidence pointers"
      min_lines: 30
      contains: "PROD-01"
  key_links:
    - from: "web/src + web/public/mockups.css"
      to: "web/public/"
      via: "parity-ref-check normalize → disk exists (Assert A)"
      pattern: "web/public"
    - from: ".planning/phases/05-prune-public-assets/05-DELETE-LIST.txt"
      to: "refs ∩ delete = ∅ and paths absent"
      via: "Assert B in parity-ref-check.mjs"
      pattern: "05-DELETE-LIST"
    - from: ".planning/phases/02-asset-inventory/02-INVENTORY.json"
      to: "keep paths on disk"
      via: "Assert C class=keep exists"
      pattern: "\"class\": \"keep\""
    - from: "web/"
      to: "npm run build"
      via: "Assert D production build"
      pattern: "npm run build"
    - from: "DominionHome.tsx"
      to: "CinematicHero + homeHtml + initDominionMockups"
      via: "runtime composition unchanged unless D-04"
      pattern: "initDominionMockups"
---

<objective>
Prove **PROD-01**: after Phase 5 prune, remaining live homepage sections still match pre-cleanup look/behavior for shipped content — via path-aware ref integrity, delete/keep SoT asserts, green `npm run build`, and a structured parity checklist (optional browser smoke).

Purpose: Lock verification evidence before mockups archive (Phases 7–9). No redesign; D-04 minimal fix only if prune caused a missing keep ref.
Output: `scripts/parity-ref-check.mjs` (exit 0), green build, `06-PARITY-CHECKLIST.md` with automated rows filled, `06-01-SUMMARY.md` citing PROD-01.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-parity-verification/06-CONTEXT.md
@.planning/phases/06-parity-verification/06-UI-SPEC.md
@.planning/phases/06-parity-verification/06-RESEARCH.md
@.planning/phases/02-asset-inventory/02-INVENTORY.json
@.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt
@.planning/phases/05-prune-public-assets/05-01-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Runtime composition + contracts — use directly; do not redesign -->

From web/src/components/DominionHome.tsx:
```typescript
export function DominionHome() {
  // useEffect → dynamic import("@/lib/mockups-main.js").then(mod => mod.initDominionMockups())
  // renders <CinematicHero /> + dangerouslySetInnerHTML={{ __html: homeHtml }}
}
```

From web/src/content/home-html.ts:
```typescript
export const homeHtml = "..."; // HTML string with section ids: proof, awards(hidden), sessions, markets, quotes, funding, trader-community, mrkt, mt5-simulator, how-it-works, accounts-focus, accounts, platforms, why-dominion, dashboard, trade-better, blogs, ecosystem, copy-trading, trust, final-cta + footer
```

From web/src/lib/mockups-main.js:
```javascript
export function initDominionMockups() { /* early-return if roots missing — silent no-op OK */ }
export default initDominionMockups;
```

From web/src/components/CinematicHero.tsx (keep asset URLs):
```typescript
// /assets/hero/Hero-background.png
// /assets/logo/logo-horizontal-reverse.svg
// /assets/awards/laurel-wreath.svg
export function CinematicHero()
```

Inventory SoT (02-INVENTORY.json):
```json
{ "summary": { "keep": 82, "delete": 55 }, "files": [{ "path": "web/public/...", "bytes": N, "class": "keep"|"delete", "evidence": "..." }] }
```

DELETE SoT: `.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt` — 55 lines, full paths like `web/public/assets/...` (path-aware only; never basename).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author parity-ref-check + run Asserts A/B/C (D-04 if needed)</name>
  <files>.planning/phases/06-parity-verification/scripts/parity-ref-check.mjs, web/src/content/home-html.ts, web/src/components/CinematicHero.tsx, web/src/app/layout.tsx, web/public/mockups.css</files>
  <action>
Create committed one-shot Node script at `.planning/phases/06-parity-verification/scripts/parity-ref-check.mjs` (phase `scripts/` preferred per research / D-01 discretion — **not** under `web/`).

**Extract sources (only):** walk all files under `web/src/` + read `web/public/mockups.css`. Do **not** scan `mockups/` or `docs/` as keep proof (D-03).

**Normalize rules (per RESEARCH Pattern 1 — mandatory):**
- Regex: `/\/(?:assets\/[A-Za-z0-9_./\-]+|mockups\.css)/g`
- Strip `?query` and `#hash` before disk map
- Strip trailing `\` from TS string escapes (`src=\"/assets/...\"`)
- Map URL → disk: `web/public` + URL path (e.g. `/assets/foo` → `web/public/assets/foo`)
- Resolve all paths under repo root; reject `..` (T-06-01)

**Asserts (all must pass for exit 0):**
- **A:** every normalized ref exists on disk; print unique ref count (expect ≥ 80; research baseline 82)
- **B:** refs ∩ lines from `05-DELETE-LIST.txt` = ∅; **and** every delete-list path `!fs.existsSync` (55 absent)
- **C:** every `02-INVENTORY.json` entry with `class === "keep"` exists on disk (82 keeps)

**Anti-patterns to avoid:** basename-only delete matching (collisions: `commodities.svg`, `discord.svg`, `funding.png`, `mist-bg.jpg`); treating `\`-suffixed paths as missing.

Run from repo root:
`node .planning/phases/06-parity-verification/scripts/parity-ref-check.mjs`

**D-04 conditional (same task):** If Assert A fails on a keep path that is prune-caused (missing because Phase 5 deleted a still-referenced file, or caller still points at a delete-list path): apply the **smallest** caller/path fix in `home-html.ts` / `CinematicHero.tsx` / `layout.tsx` / `mockups.css` — retarget to an existing keep path or remove the broken ref. **Never** restore files from `mockups/` or re-add Phase 5 deletes. If failure is pre-existing / out of inventory scope → record in notes only; do not redesign (D-04). Re-run script until exit 0. If all asserts already green → **do not** modify any `web/` files.
  </action>
  <verify>
    <automated>cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs" && node .planning/phases/06-parity-verification/scripts/parity-ref-check.mjs</automated>
  </verify>
  <done>Script committed under phase scripts/; exit 0 with 0 missing refs, 0 ∩ delete list, 55 deletes absent, 82 keeps present; D-04 applied only if prune-caused break found.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Production build + write 06-PARITY-CHECKLIST.md</name>
  <files>.planning/phases/06-parity-verification/06-PARITY-CHECKLIST.md</files>
  <action>
**Assert D (D-01):** From repo root run `cd web && npm run build` — must exit 0. No new test frameworks (no Playwright/Vitest).

**Write** `.planning/phases/06-parity-verification/06-PARITY-CHECKLIST.md` as hybrid evidence (D-01 layer 2 / UI-SPEC). Use a markdown table with columns: `Surface | Expectation | Method (auto/human) | Status | Notes`.

**Required sections / rows (order locked by UI-SPEC §2):**

1. **Meta / automated** (fill Status=PASS from Task 1 + this build; Method=auto):
   - Ref integrity script (Assert A)
   - DELETE-LIST unreferenced + absent (Assert B)
   - Inventory keeps present (Assert C)
   - `npm run build` PASS (Assert D)

2. **Cinematic hero + nav** (`CinematicHero.tsx`):
   - Hero BG `/assets/hero/Hero-background.png` — Method=auto if covered by ref-check else human
   - Logo `/assets/logo/logo-horizontal-reverse.svg`
   - Nav anchors exist targeting: `#markets`, `#sessions`, `#accounts`, `#funding`, `#platforms`, `#dashboard`, `#copy-trading`, `#ecosystem`, `#why-dominion`, `#trust`, `#blogs`, `#final-cta`, `#top`

3. **Body sections** (one row each — DOM present; keep images not broken; Method=auto for “section id present in home-html.ts” via grep/script note, Method=human for visual layout):
   proof → awards *(note: `hidden` OK — parity = stay hidden)* → sessions → markets → quotes → funding → trader-community → mrkt → mt5-simulator → how-it-works → accounts-focus → accounts → platforms → why-dominion → dashboard → trade-better → blogs → ecosystem → copy-trading → trust → final-cta → footer

4. **Widgets** (UI-SPEC §4 — Method=human or auto-init-smoke; do **not** fail on missing `#site-nav` / `data-xhero` — pre-existing React hero no-ops):
   - `[data-marquee]` tracks (proof / markets / trader-community) — no init throw
   - `#markets` `[data-platform]` tabs
   - Quotes `[data-quotes-tab]`
   - `#dashboard` `[data-dash-feature]` tabs
   - Ecosystem tabs under `#ecosystem` if present

5. **CSS backgrounds** — note that static scan covers `mockups.css` `url(/assets/...)`; visual optional

**Fill automated rows now** with PASS + notes citing script output (ref count) and build exit 0. Leave true visual/interaction rows as `human_needed` (or PASS only if Task 3 smoke provides evidence). **No marketing copy changes. No new components/tokens** unless Task 1 already applied D-04.
  </action>
  <verify>
    <automated>cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs" && cd web && npm run build && test -f "../.planning/phases/06-parity-verification/06-PARITY-CHECKLIST.md" && grep -v '^#' "../.planning/phases/06-parity-verification/06-PARITY-CHECKLIST.md" | grep -c 'final-cta' | awk '{exit ($1<1)?1:0}' && grep -v '^#' "../.planning/phases/06-parity-verification/06-PARITY-CHECKLIST.md" | grep -E 'PASS|pass' | head -1 && grep -v '^#' "../.planning/phases/06-parity-verification/06-PARITY-CHECKLIST.md" | grep -E 'human_needed|human' | head -1</automated>
  </verify>
  <done>Build exit 0; checklist file exists with hero + all body sections through final-cta/footer + widgets; automated meta rows PASS; visual/widget rows human_needed unless smoke later fills them.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Optional D-02 smoke + PROD-01 SUMMARY</name>
  <files>.planning/phases/06-parity-verification/06-01-SUMMARY.md, .planning/phases/06-parity-verification/06-PARITY-CHECKLIST.md</files>
  <action>
**D-02 (discretionary, non-blocking):** If browser MCP / IDE browser tools are available after Task 2 build: `cd web && npm run start -- -p 3000`, open `http://localhost:3000/`, spot-check desktop — hero logo/BG, `#proof` marquee, one `#markets` platform tab, one `#dashboard` tab; optionally one mobile width. Attach notes (and screenshot paths if taken) into checklist rows; mark those rows PASS. Prefer `next start` over `next dev`. If browser tools unavailable or smoke fails for non-prune reasons → **skip**; leave `human_needed`; do not fail the plan.

**Write** `.planning/phases/06-parity-verification/06-01-SUMMARY.md` per summary template:
- State **PROD-01** evidence: script exit 0 (ref count, 0 missing, 0 ∩ delete), 55 deletes absent, 82 keeps present, build exit 0, checklist path
- List any D-04 code changes (or “none — asserts green”)
- List `human_needed` checklist rows remaining for verifier/UAT
- Note whether D-02 smoke ran or was skipped
- Do **not** treat `mockups/` as visual baseline (D-03)
- Do **not** mark ROADMAP/STATE phase complete (orchestrator owns)
  </action>
  <verify>
    <automated>cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs" && test -f .planning/phases/06-parity-verification/06-01-SUMMARY.md && grep -v '^#' .planning/phases/06-parity-verification/06-01-SUMMARY.md | grep -c 'PROD-01' | awk '{exit ($1<1)?1:0}' && node .planning/phases/06-parity-verification/scripts/parity-ref-check.mjs && cd web && npm run build</automated>
  </verify>
  <done>SUMMARY exists with PROD-01 pointers; final script+build still green; D-02 optional notes recorded or explicitly skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → filesystem script | Ref-check reads fixed roots under repo; no untrusted user path input |
| Client → static `/assets/*` | Public marketing assets only; no auth |
| Planning artifacts → delete/keep gates | SoT files must not be rewritten to “pass” checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `scripts/parity-ref-check.mjs` | mitigate | Resolve paths under repo root only; reject `..` in normalized paths; read only `web/src` + `web/public/mockups.css` |
| T-06-02 | T | D-04 fix path | mitigate | Never copy from `mockups/` or restore `05-DELETE-LIST` binaries to fake parity (D-03/D-04) |
| T-06-03 | I | phase scripts / SUMMARY | accept | No secrets required; filesystem-only verification; low-value marketing paths |
| T-06-04 | R | checklist Status column | mitigate | Automated rows must cite script/build evidence; visual PASS only with smoke notes or human_needed |
| T-06-05 | E | inventing deletes as keeps | mitigate | Assert B enforces delete-list absence; do not edit DELETE-LIST or INVENTORY to pass |
</threat_model>

<verification>
- `node .planning/phases/06-parity-verification/scripts/parity-ref-check.mjs` exit 0
- All 55 paths in `05-DELETE-LIST.txt` absent; 82 inventory keeps present
- `cd web && npm run build` exit 0
- `06-PARITY-CHECKLIST.md` present with automated PASS + section/widget coverage
- `06-01-SUMMARY.md` cites PROD-01
</verification>

<success_criteria>
PROD-01 satisfied when remaining live homepage content has proven asset integrity (no missing keep refs, no lingering delete refs), production build succeeds, and structured checklist documents hero + shipped sections + widgets — with D-04 only if prune caused a break, and D-02 smoke optional.
</success_criteria>

<output>
After completion, create `.planning/phases/06-parity-verification/06-01-SUMMARY.md`
</output>
