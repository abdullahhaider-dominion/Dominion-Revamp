---
phase: 08-retarget-design-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .cursor/skills/dominion-section-redesign/SKILL.md
  - readme.md
  - web/README.md
  - .planning/PROJECT.md
  - CLAUDE.md
  - .planning/phases/08-retarget-design-docs/08-01-SUMMARY.md
autonomous: true
requirements:
  - MOCK-02
user_setup: []

must_haves:
  truths:
    - "Skill cites 07-ARCHIVE.md and does not list Working hi-fi: mockups/ as required SoT"
    - "readme.md and web/README.md link 07-ARCHIVE.md and do not declare in-repo mockups/ as current design SoT"
    - "PROJECT.md Context no longer says Design SoT today: mockups/"
    - "CLAUDE.md no longer says Design source of truth remains mockups/"
    - "Deny-list rg is clean on all five target files; allow-list finds 07-ARCHIVE.md in skill + READMEs + PROJECT"
    - "git ls-files mockups still 139; no web/src changes; no git push; mockups/ not deleted"
  artifacts:
    - path: ".cursor/skills/dominion-section-redesign/SKILL.md"
      provides: "Agent redesign workflow pointing at archive + web/ production"
      min_lines: 80
      contains: "07-ARCHIVE.md"
    - path: "readme.md"
      provides: "Root user-facing SoT pointer"
      contains: "07-ARCHIVE.md"
    - path: "web/README.md"
      provides: "web/ Notes SoT pointer (relative link to ARCHIVE)"
      contains: "07-ARCHIVE.md"
    - path: ".planning/PROJECT.md"
      provides: "Project context SoT line"
      contains: "07-ARCHIVE.md"
    - path: "CLAUDE.md"
      provides: "Operational agent SoT no longer mockups-first"
      min_lines: 50
    - path: ".planning/phases/08-retarget-design-docs/08-01-SUMMARY.md"
      provides: "Grep proof + D-05 attestations"
      min_lines: 25
      contains: "MOCK-02"
  key_links:
    - from: ".cursor/skills/dominion-section-redesign/SKILL.md"
      to: ".planning/phases/07-archive-mockups/07-ARCHIVE.md"
      via: "Source of truth + frontmatter cite ARCHIVE"
      pattern: "07-ARCHIVE\\.md"
    - from: "readme.md"
      to: ".planning/phases/07-archive-mockups/07-ARCHIVE.md"
      via: "markdown link / path"
      pattern: "07-ARCHIVE\\.md"
    - from: "web/README.md"
      to: "../.planning/phases/07-archive-mockups/07-ARCHIVE.md"
      via: "relative link from web/"
      pattern: "07-ARCHIVE\\.md"
    - from: ".planning/PROJECT.md"
      to: ".planning/phases/07-archive-mockups/07-ARCHIVE.md"
      via: "Context line"
      pattern: "07-ARCHIVE\\.md"
---

<objective>
Satisfy **MOCK-02**: retarget operational docs and the redesign skill so they no longer treat in-repo `mockups/` as the required working design SoT. Point hi-fi work at Phase 7 archive coordinates; point production polish at `web/`.

Purpose: Unblock Phase 9 delete — agents/humans must stop requiring local `mockups/` edits.
Output: Updated skill, `readme.md`, `web/README.md`, `.planning/PROJECT.md`, `CLAUDE.md`, plus `08-01-SUMMARY.md` with grep + retain proof.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-retarget-design-docs/08-CONTEXT.md
@.planning/phases/08-retarget-design-docs/08-RESEARCH.md
@.planning/phases/08-retarget-design-docs/08-VALIDATION.md
@.planning/phases/07-archive-mockups/07-ARCHIVE.md

<interfaces>
<!-- Locked wording + gates — use directly; do not invent alternate SoT paths -->

Canonical one-liner (prefer per D-01…D-03 wording policy):
```text
Hi-fi design archive: see `.planning/phases/07-archive-mockups/07-ARCHIVE.md` (`archive/mockups` / sibling repo). Production site: `web/`.
```

Archive link target (read-only — do not rewrite Phase 7 record):
```text
.planning/phases/07-archive-mockups/07-ARCHIVE.md
```
From `web/README.md` use relative: `../.planning/phases/07-archive-mockups/07-ARCHIVE.md`

Hard stops (D-04, D-05):
- Do **not** delete `mockups/` (Phase 9)
- Do **not** `git push`
- Do **not** edit `web/src/**`
- Do **not** rewrite historical `docs/superpowers/specs/*` or `docs/superpowers/plans/*`
- Do **not** create `RETARGET.md` (SUMMARY + grep enough)
- Skip `.planning/codebase/*` remapping

Target files only:
```text
.cursor/skills/dominion-section-redesign/SKILL.md
readme.md
web/README.md
.planning/PROJECT.md
CLAUDE.md
```

Deny-list phrases (must be absent after retarget):
- `Source of truth for design still lives in \`../mockups/\``
- `Design SoT today: \`mockups/\``
- `Working hi-fi: \`mockups/\``
- `Design source of truth remains \`mockups/\``
- `Vanilla hi-fi mockup lives in \`mockups/\``

Provenance OK: “ported from mockups/…”, temporary “may exist until Phase 9 — not required SoT”.
</interfaces>
</context>

<tasks>

<task type="auto" id="08-01-01">
  <name>Task 1: Retarget dominion-section-redesign skill (D-01)</name>
  <files>.cursor/skills/dominion-section-redesign/SKILL.md</files>
  <action>
Rewrite `.cursor/skills/dominion-section-redesign/SKILL.md` so working hi-fi / implement paths cite the archive, not in-repo `mockups/` as required SoT (per **D-01**).

Exact changes:
1. **Frontmatter `description`:** Use RESEARCH recommended text (or near-exact): redesign Floating Sanctuary sections; archive hi-fi via `07-ARCHIVE.md`; production polish in `web/`. Remove “implementing glass UI in mockups/” as the only path.
2. **When to use:** Replace “Implementing or polishing sections inside \`mockups/\`” with archive checkout / sibling **or** production polish under `web/`.
3. **Source of truth block:** Replace items 3–4. Must include:
   - Hi-fi design archive → `.planning/phases/07-archive-mockups/07-ARCHIVE.md` — branch `archive/mockups` + sibling (coordinates in that file). Do **not** treat in-repo `mockups/` as required SoT (may remain until Phase 9).
   - Production polish: `web/` (Next.js App Router)
   - Keep brand + design-spec lines 1–2.
   - Concepts: “Save under archive \`concepts/\` when useful” — not “must write to in-repo \`mockups/concepts/\`”.
4. **§4 Implement:** Prefer implement/polish in archive working tree (sibling / \`archive/mockups\` checkout) **or** \`web/\` when shipping. Avoid “Add/update section in \`mockups/index.html\`” as the only path. Logo assets: cite archive assets path or \`web/public/assets/\` — not “use only in-repo \`mockups/assets/logo/\`” as mandatory SoT.
5. **Output checklist:** Replace “Implemented in \`mockups/\` with cache bump” with archive and/or \`web/\` equivalent.

Do not delete \`mockups/\`. Do not edit other files in this task. Link ARCHIVE by path string \`07-ARCHIVE.md\` (full relative path OK). Do not copy tip SHA into the skill — link ARCHIVE only (Pattern 1).
  </action>
  <verify>
    <automated><![CDATA[
cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs"
rg -q '07-ARCHIVE\.md' .cursor/skills/dominion-section-redesign/SKILL.md
! rg -n -e 'Working hi-fi: `mockups/`' .cursor/skills/dominion-section-redesign/SKILL.md
! rg -n -e 'Source of truth for design still lives in `\.\./mockups/`' -e 'Design SoT today: `mockups/`' -e 'Working hi-fi: `mockups/`' -e 'Design source of truth remains `mockups/`' -e 'Vanilla hi-fi mockup lives in `mockups/`' .cursor/skills/dominion-section-redesign/SKILL.md
]]></automated>
  </verify>
  <done>Skill cites 07-ARCHIVE.md; no Working hi-fi: mockups/ SoT line; implement path is archive and/or web/, not in-repo mockups-only.</done>
</task>

<task type="auto" id="08-01-02">
  <name>Task 2: Retarget READMEs, PROJECT.md, CLAUDE.md (D-02, D-03)</name>
  <files>readme.md, web/README.md, .planning/PROJECT.md, CLAUDE.md</files>
  <action>
Update operational SoT declarations (per **D-02**, **D-03**; CLAUDE.md per RESEARCH YOLO / MOCK-02 “any other docs”).

1. **`readme.md`:** Replace “Vanilla hi-fi mockup lives in \`mockups/\`…” with RESEARCH pattern:
   - Production Next.js site: \`web/\`
   - Hi-fi design archive: link \`.planning/phases/07-archive-mockups/07-ARCHIVE.md\` (\`archive/mockups\` / sibling)
   - In-repo \`mockups/\` may exist temporarily until Phase 9 — **not** required design SoT

2. **`web/README.md`:** Replace Notes bullet “Source of truth for design still lives in \`../mockups/\`.” with ARCHIVE link using relative path \`../.planning/phases/07-archive-mockups/07-ARCHIVE.md\`. Keep historical port bullets (“Interactions come from the ported…”, “copied from mockups/assets/”) — provenance, not SoT. Opening line may still say “port of the vanilla mockups/ homepage” as history.

3. **\`.planning/PROJECT.md\`:** Replace Context line “Design SoT today: \`mockups/\`…” with:
   - Production app: \`web/\`. Hi-fi design archive: \`.planning/phases/07-archive-mockups/07-ARCHIVE.md\` (\`archive/mockups\` / sibling). In-repo \`mockups/\` may remain until Phase 9 — not required SoT.
   Do not mark Phase 8 Active checklist complete here (executor SUMMARY / later transition owns that).

4. **\`CLAUDE.md\`:** Replace all “Design source of truth remains \`mockups/\`” occurrences with the canonical ARCHIVE + \`web/\` one-liner (or equivalent). Soften Project Skills table description to match skill frontmatter. Soften Entry Points / Pattern Overview language that pushes \`npx serve mockups\` as the mandatory design SoT entry — may note temporary preview until Phase 9 or point at ARCHIVE sibling. Keep factual “ported from mockups” history. Reword empty/anti-pattern heading “Editing Next styles as the design source of truth” if it still implies mockups-first is mandatory.

Hard stops: no \`web/src\` edits; no \`docs/superpowers/**\` rewrites (**D-04**); no \`mockups/\` delete (**D-05**); no \`git push\`.
  </action>
  <verify>
    <automated><![CDATA[
cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs"
! rg -n \
  -e 'Source of truth for design still lives in `\.\./mockups/`' \
  -e 'Design SoT today: `mockups/`' \
  -e 'Working hi-fi: `mockups/`' \
  -e 'Design source of truth remains `mockups/`' \
  -e 'Vanilla hi-fi mockup lives in `mockups/`' \
  readme.md web/README.md .planning/PROJECT.md CLAUDE.md
for f in readme.md web/README.md .planning/PROJECT.md; do
  rg -q '07-ARCHIVE\.md' "$f" || { echo "MISSING ARCHIVE link: $f"; exit 1; }
done
echo "TASK2: OK"
]]></automated>
  </verify>
  <done>Deny-list clean on READMEs/PROJECT/CLAUDE; ARCHIVE linked from readme, web/README, PROJECT; no web/src changes.</done>
</task>

<task type="auto" id="08-01-03">
  <name>Task 3: Full Nyquist gate + SUMMARY (MOCK-02 proof)</name>
  <files>.planning/phases/08-retarget-design-docs/08-01-SUMMARY.md</files>
  <action>
Run the full VALIDATION suite from repo root and write \`08-01-SUMMARY.md\` with evidence (skip optional RETARGET.md per discretion).

Full suite (must all print OK):
```bash
cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs"
# 1) deny must be empty (exit success via !)
! rg -n \
  -e 'Source of truth for design still lives in `\.\./mockups/`' \
  -e 'Design SoT today: `mockups/`' \
  -e 'Working hi-fi: `mockups/`' \
  -e 'Design source of truth remains `mockups/`' \
  -e 'Vanilla hi-fi mockup lives in `mockups/`' \
  readme.md web/README.md .planning/PROJECT.md \
  .cursor/skills/dominion-section-redesign/SKILL.md CLAUDE.md \
  && echo "DENY: OK"
# 2) allow
for f in readme.md web/README.md .planning/PROJECT.md .cursor/skills/dominion-section-redesign/SKILL.md; do
  rg -q '07-ARCHIVE\.md' "$f" || exit 1
done && echo "ALLOW: OK"
# 3) retain
test "$(git ls-files mockups | wc -l | tr -d ' ')" = "139" && echo "RETAIN: OK"
```

SUMMARY must include:
- Requirement **MOCK-02** satisfied
- Files changed list (skill, readme.md, web/README.md, PROJECT.md, CLAUDE.md)
- Paste or paraphrase DENY/ALLOW/RETAIN results
- Attestations: no \`git push\`; no \`mockups/\` delete; no \`web/src\` in commit set; no \`docs/superpowers\` rewrites (**D-04**, **D-05**)
- Decisions honored: D-01…D-05

Do not mark ROADMAP Phase 8 complete. Do not edit \`web/src\`.
  </action>
  <verify>
    <automated><![CDATA[
cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs"
! rg -n \
  -e 'Source of truth for design still lives in `\.\./mockups/`' \
  -e 'Design SoT today: `mockups/`' \
  -e 'Working hi-fi: `mockups/`' \
  -e 'Design source of truth remains `mockups/`' \
  -e 'Vanilla hi-fi mockup lives in `mockups/`' \
  readme.md web/README.md .planning/PROJECT.md \
  .cursor/skills/dominion-section-redesign/SKILL.md CLAUDE.md \
  && echo "DENY: OK"
for f in readme.md web/README.md .planning/PROJECT.md .cursor/skills/dominion-section-redesign/SKILL.md; do
  rg -q '07-ARCHIVE\.md' "$f" || { echo "MISSING: $f"; exit 1; }
done && echo "ALLOW: OK"
test "$(git ls-files mockups | wc -l | tr -d ' ')" = "139" && echo "RETAIN: OK"
test -f .planning/phases/08-retarget-design-docs/08-01-SUMMARY.md
rg -q 'MOCK-02' .planning/phases/08-retarget-design-docs/08-01-SUMMARY.md
]]></automated>
  </verify>
  <done>Full deny+allow+retain green; SUMMARY exists with MOCK-02 + D-05 attestations; mockups still 139 tracked files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent/human → docs/skills | Untrusted readers follow SoT pointers; wrong SoT causes bad edits |
| Executor → git tree | Accidental delete of `mockups/` or `git push` / `web/src` edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Skill + README SoT lines | mitigate | Deny-list + allow-list `rg` gates; every SoT rewrite links `07-ARCHIVE.md` only |
| T-08-02 | Tampering / Denial | `mockups/` tree | mitigate | D-05 hard stop; `git ls-files mockups` must remain 139; SUMMARY attestation |
| T-08-03 | Information Disclosure | `git push` of archive/branch | mitigate | D-05 forbid push; SUMMARY negative attestation |
| T-08-04 | Tampering | `web/src` runtime | mitigate | Phase file list excludes `web/src`; verify no src in commit set |
| T-08-05 | Repudiation | Historical docs rewrite | accept | D-04 leave-alone; out of gate scope for `docs/superpowers/**` |
</threat_model>

<verification>
From repo root, full VALIDATION suite (deny + allow + retain) must print DENY/ALLOW/RETAIN OK. Confirm `08-01-SUMMARY.md` present. Spot-check skill §Implement does not require only in-repo `mockups/index.html`.
</verification>

<success_criteria>
- MOCK-02: operational docs/skills no longer declare in-repo `mockups/` as required design SoT
- D-01…D-05 honored (skill + READMEs + PROJECT + CLAUDE retargeted; history untouched; no delete/push/web/src)
- Nyquist grep gates from `08-VALIDATION.md` green
</success_criteria>

<output>
After completion, create `.planning/phases/08-retarget-design-docs/08-01-SUMMARY.md`
</output>
