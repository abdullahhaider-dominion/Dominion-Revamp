---
phase: 01-track-web-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/.gitignore
  - web/package.json
  - web/package-lock.json
  - web/next.config.ts
  - web/tsconfig.json
  - web/eslint.config.mjs
  - web/postcss.config.mjs
  - web/README.md
  - web/AGENTS.md
  - web/CLAUDE.md
  - web/src/
  - web/public/
autonomous: true
requirements:
  - PROD-02
user_setup: []

must_haves:
  truths:
    - "web/.gitignore excludes node_modules, .next, .env*, .DS_Store, next-env.d.ts, and *.tsbuildinfo before any web/ commit"
    - "web/src, web/public (full tree including later prune candidates), and app config are tracked in git — not left as untracked ?? web/"
    - "Phase 1 commit contains only paths under web/ — no repo-root Hero-background.png or root .DS_Store"
    - "A fresh checkout can obtain app source from git without relying solely on mockups/"
  artifacts:
    - path: "web/.gitignore"
      provides: "Ignore rules for dependencies, build output, env, OS junk, generated TS"
      contains: "node_modules"
    - path: "web/package.json"
      provides: "App identity and scripts"
      contains: "\"name\": \"web\""
    - path: "web/src/app/page.tsx"
      provides: "App Router home entry"
      min_lines: 1
    - path: "web/public/mockups.css"
      provides: "Served Floating Sanctuary stylesheet"
      min_lines: 1
    - path: "web/public/assets/hero/Hero-background.png"
      provides: "Live cinematic hero asset (tracked per D-01)"
  key_links:
    - from: "web/.gitignore"
      to: "git index"
      via: "git add web/ respects ignore rules"
      pattern: "/node_modules|/.next/|\\.env\\*"
    - from: "git commit (web/ only)"
      to: "PROD-02 tracked tree"
      via: "git ls-files web/"
      pattern: "^web/(src|public|package\\.json)"
---

<objective>
Safely track the production Next.js app under `web/` in git: verify ignore rules, commit the full `web/` source + public + config tree (including all of `web/public`), and confirm secrets/build artifacts and repo-root orphans stay out.

Purpose: Unblocks every later cleanup phase — inventory, deletes, and parity all need a git-tracked `web/` tree (PROD-02).
Output: One coherent commit adding `web/` paths; SUMMARY documenting verification commands and commit SHA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-track-web-app/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@web/.gitignore
@web/package.json

## Locked decisions (NON-NEGOTIABLE)

- **D-01:** Track **all** of `web/public` now (including Phase 5 prune candidates such as `web/public/assets/hero/hero.mp4`). Do not exclude large orphans from this commit.
- **D-02:** Commit **only** paths under `web/`. Do **not** `git add` repo-root `Hero-background.png`, root `.DS_Store`, `docs/`, `mockups/`, or other non-`web/` untracked paths.

## Claude's Discretion (apply as written)

- Single coherent commit for the `web/` add.
- Rely on existing `web/.gitignore` — do not create a root `.gitignore` for Phase 1 success.
- Keep `next-env.d.ts` and `*.tsbuildinfo` ignored (do not force-add them).

## Current repo state (at plan time)

- Branch: `main` (ahead of origin); `web/` is entirely untracked (`?? web/`).
- `web/node_modules` and `web/.next` exist on disk and must remain untracked.
- Untracked at repo root (OUT OF SCOPE for this commit): `Hero-background.png`, `.DS_Store`.

## Source coverage audit

| SOURCE | ID | Item | Plan | Status |
|--------|----|------|------|--------|
| GOAL | — | Production Next.js app under `web/` safely tracked (no node_modules/.next) | 01 | COVERED |
| REQ | PROD-02 | Ensure `web/` app source is tracked in git | 01 | COVERED |
| RESEARCH | — | Skipped by user | — | N/A |
| CONTEXT | D-01 | Track all of `web/public` | 01 | COVERED |
| CONTEXT | D-02 | Commit only paths under `web/` | 01 | COVERED |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify web/.gitignore excludes secrets and build artifacts</name>
  <files>web/.gitignore</files>
  <read_first>
    web/.gitignore
    .planning/phases/01-track-web-app/01-CONTEXT.md
    .planning/ROADMAP.md
  </read_first>
  <action>
    From repo root `/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs`, verify existing `web/.gitignore` already covers Phase 1 success criterion 1. Do **not** edit ignore rules unless a check below fails.

    Required patterns already present in `web/.gitignore` (confirm by reading the file):
    - `/node_modules` (dependencies)
    - `/.next/` (Next build)
    - `.env*` (env secrets)
    - `.DS_Store` (OS junk)
    - `next-env.d.ts` and `*.tsbuildinfo` (generated TS — keep ignored per discretion)

    Run these exact checks (all must exit 0 and print a matching rule):

    ```bash
    git check-ignore -v web/node_modules
    git check-ignore -v web/.next
    git check-ignore -v web/.env.local
    git check-ignore -v web/.DS_Store
    git check-ignore -v web/next-env.d.ts
    git check-ignore -v web/tsconfig.tsbuildinfo
    ```

    Also confirm live hero + served CSS exist on disk (will be tracked in Task 2 per D-01):

    ```bash
    test -f web/public/assets/hero/Hero-background.png
    test -f web/public/mockups.css
    test -d web/src
    test -f web/package.json
    ```

    If any `git check-ignore` fails for the paths above, fix `web/.gitignore` minimally so those paths are ignored, then re-run the checks. Do not broaden scope to root ignores.
  </action>
  <verify>
    <automated>cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs" && git check-ignore -v web/node_modules web/.next web/.env.local web/.DS_Store web/next-env.d.ts web/tsconfig.tsbuildinfo && test -f web/public/assets/hero/Hero-background.png && test -f web/public/mockups.css && test -d web/src && test -f web/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore -v web/node_modules` exits 0 and output contains `node_modules`
    - `git check-ignore -v web/.next` exits 0 and output contains `.next`
    - `git check-ignore -v web/.env.local` exits 0 and output contains `.env`
    - `git check-ignore -v web/.DS_Store` exits 0 and output contains `.DS_Store`
    - `git check-ignore -v web/next-env.d.ts` exits 0
    - `git check-ignore -v web/tsconfig.tsbuildinfo` exits 0
    - `test -f web/public/assets/hero/Hero-background.png` exits 0
    - `test -f web/public/mockups.css` exits 0
    - File `web/.gitignore` contains the literal strings `/node_modules`, `/.next/`, and `.env*`
  </acceptance_criteria>
  <done>Ignore rules verified for node_modules, .next, .env*, OS junk, and generated TS; required web source/public paths exist on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Stage and commit only web/ (full public tree)</name>
  <files>web/</files>
  <read_first>
    web/.gitignore
    .planning/phases/01-track-web-app/01-CONTEXT.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    From repo root, add and commit **only** the `web/` tree in one coherent commit (discretion + D-01 + D-02).

    **Do NOT add** (explicit deny-list for this phase):
    - `Hero-background.png` (repo root — Phase 3)
    - `.DS_Store` or `docs/.DS_Store` / `mockups/.DS_Store` at repo root or outside `web/`
    - `docs/`, `mockups/`, `wireframes/` changes
    - `readme.md` modifications outside this phase
    - Force-adding ignored paths (`web/node_modules`, `web/.next`, `web/.env*`, `web/next-env.d.ts`, `web/*.tsbuildinfo`)

    **Do add** (per D-01 / PROD-02):
    - Entire `web/` directory via `git add web/` so `web/src/`, **all** of `web/public/` (including prune candidates), and app config (`package.json`, `package-lock.json`, `next.config.ts`, `tsconfig.json`, `eslint.config.mjs`, `postcss.config.mjs`, `.gitignore`, READMEs) are staged. Rely on `web/.gitignore` to exclude ignored files automatically.

    Exact sequence:

    ```bash
    cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs"

    # Preview (optional sanity): should list web/ paths only
    git add -n web/

    # Stage only web/
    git add web/

    # Hard gates before commit — must all pass (exit 0)
    # 1) No non-web paths in the staged set for this commit intent:
    test -z "$(git diff --cached --name-only | grep -v '^web/' || true)"

    # 2) No secrets / build / generated artifacts staged:
    test -z "$(git diff --cached --name-only | grep -E '(^|/)node_modules(/|$)|(^|/)\.next(/|$)|(^|/)\.env|\.pem$|next-env\.d\.ts$|\.tsbuildinfo$' || true)"

    # 3) Required tracked candidates are staged:
    git diff --cached --name-only | grep -qx 'web/package.json'
    git diff --cached --name-only | grep -q '^web/src/'
    git diff --cached --name-only | grep -q '^web/public/'
    git diff --cached --name-only | grep -qx 'web/public/mockups.css'
    git diff --cached --name-only | grep -qx 'web/public/assets/hero/Hero-background.png'
    # D-01: at least one known later prune candidate is included (honest full public snapshot)
    git diff --cached --name-only | grep -qx 'web/public/assets/hero/hero.mp4' || test ! -f web/public/assets/hero/hero.mp4

    # 4) Root orphans must NOT be staged:
    test -z "$(git diff --cached --name-only | grep -E '^(Hero-background\.png|\.DS_Store)$' || true)"

    git commit -m "$(cat <<'EOF'
    chore(web): track Next.js app source and public assets

    Add web/src, full web/public, and app config to git using existing
    web/.gitignore so node_modules, .next, env files, and generated
    TypeScript artifacts stay untracked (PROD-02).

    EOF
    )"
    ```

    If `hero.mp4` exists on disk but is missing from the staged set, stop and fix staging (D-01 requires full `web/public`). If it does not exist on disk, the `test ! -f` branch allows continue.

    After commit, record the commit SHA in the SUMMARY.
  </action>
  <verify>
    <automated>cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs" && git rev-parse --verify HEAD && test -z "$(git diff --cached --name-only)" && git ls-files web/package.json web/src/app/page.tsx web/public/mockups.css web/public/assets/hero/Hero-background.png | wc -l | grep -q '[[:space:]]*4$' && test "$(git ls-files 'web/node_modules/*' 'web/.next/*' | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `git status -sb` does not show `?? web/` as a single untracked directory blob for the app source (web/ source paths appear as tracked)
    - `git ls-files web/package.json` prints `web/package.json`
    - `git ls-files web/src/app/page.tsx` prints `web/src/app/page.tsx`
    - `git ls-files web/public/mockups.css` prints `web/public/mockups.css`
    - `git ls-files web/public/assets/hero/Hero-background.png` prints `web/public/assets/hero/Hero-background.png`
    - `git ls-files web/ | grep -c '^web/public/'` is greater than or equal to 100
    - `git ls-files 'web/node_modules/*' | wc -l` is 0
    - `git ls-files 'web/.next/*' | wc -l` is 0
    - `git ls-files Hero-background.png` prints nothing (root orphan not tracked by this phase)
    - Latest commit message subject equals `chore(web): track Next.js app source and public assets`
  </acceptance_criteria>
  <done>Single commit exists that adds only web/ paths including full public tree and app config; ignored and root-orphan paths are absent from the index.</done>
</task>

<task type="auto">
  <name>Task 3: Prove fresh-checkout path and PROD-02 success criteria</name>
  <files>web/</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/01-track-web-app/01-CONTEXT.md
  </read_first>
  <action>
    Run final PROD-02 / roadmap success-criteria verification from repo root. Do not stage additional files.

    Exact checks (all must pass):

    ```bash
    cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs"

    # Success criterion 1 — ignores still effective
    git check-ignore -v web/node_modules web/.next web/.env.local web/.DS_Store

    # Success criterion 2 — key app paths tracked
    git ls-files --error-unmatch web/src/app/layout.tsx web/src/app/page.tsx web/src/components/DominionHome.tsx
    git ls-files --error-unmatch web/public/mockups.css web/public/assets/hero/Hero-background.png
    git ls-files --error-unmatch web/package.json web/next.config.ts web/tsconfig.json web/.gitignore

    # Not left as untracked web/ tree
    test -z "$(git status --porcelain web/ | grep '^??' || true)"

    # Success criterion 3 — app source obtainable from git without mockups/
    # (tracked web/src + web/public count > 0; mockups not required for these paths)
    test "$(git ls-files web/src | wc -l | tr -d ' ')" -ge 1
    test "$(git ls-files web/public | wc -l | tr -d ' ')" -ge 1

    # D-02 confirmation
    test -z "$(git ls-files | grep -E '^(Hero-background\.png)$' || true)"

    # D-01 confirmation when file exists on disk
    if test -f web/public/assets/hero/hero.mp4; then
      git ls-files --error-unmatch web/public/assets/hero/hero.mp4
    fi
    ```

    Write `.planning/phases/01-track-web-app/01-01-SUMMARY.md` that includes the literal substring `PROD-02`, plus: commit SHA, counts from `git ls-files web/src | wc -l` and `git ls-files web/public | wc -l`, and paste of the successful `git check-ignore -v` lines.
  </action>
  <verify>
    <automated>cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs" && git check-ignore -v web/node_modules web/.next web/.env.local web/.DS_Store && git ls-files --error-unmatch web/src/app/page.tsx web/public/mockups.css web/package.json web/.gitignore && test -z "$(git status --porcelain web/ | grep '^??' || true)" && test "$(git ls-files web/src | wc -l | tr -d ' ')" -ge 1 && test "$(git ls-files web/public | wc -l | tr -d ' ')" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch web/src/app/page.tsx` exits 0
    - `git ls-files --error-unmatch web/public/mockups.css` exits 0
    - `git ls-files --error-unmatch web/package.json` exits 0
    - `git status --porcelain web/` has zero lines matching `^??`
    - `git ls-files web/src | wc -l` is greater than or equal to 1
    - `git ls-files web/public | wc -l` is greater than or equal to 1
    - File `.planning/phases/01-track-web-app/01-01-SUMMARY.md` exists and contains the substring `PROD-02`
  </acceptance_criteria>
  <done>All three ROADMAP Phase 1 success criteria are CLI-proven; SUMMARY records SHA and file counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → git index/commit | Untrusted/local artifacts (env files, `node_modules`, OS junk, root orphans) must not cross into the committed production tree |
| Developer machine → shared remote (later) | Committed secrets would propagate to clones; Phase 1 blocks that by ignore + path scoping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `web/.env*` / secrets | mitigate | Verify `.env*` in `web/.gitignore`; `git check-ignore -v web/.env.local`; reject staged `.env` via Task 2 grep gate before commit |
| T-01-02 | Tampering / Availability | `web/node_modules`, `web/.next` | mitigate | Confirm `/node_modules` and `/.next/` ignored; assert zero `git ls-files` hits for those trees after commit |
| T-01-03 | Information Disclosure | `*.pem` / debug logs | mitigate | Existing `web/.gitignore` entries (`*.pem`, `npm-debug.log*`); staged-name gate rejects `.pem` |
| T-01-04 | Elevation of Privilege | Accidental root orphan commit (`Hero-background.png`, `.DS_Store`) | mitigate | D-02 path scope: only `git add web/`; pre-commit test that staged names all match `^web/` and exclude root orphan filenames |
| T-01-05 | Repudiation | Opaque multi-path commit | accept | Single documented commit message + SUMMARY with SHA; low risk for this hygiene phase |

Block execution only if a high-severity issue is found (e.g. `.env` or credentials would be committed). Low-severity ignore gaps: fix `.gitignore` then continue.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `git check-ignore -v web/node_modules web/.next web/.env.local web/.DS_Store` succeeds
- [ ] `git ls-files web/src web/public web/package.json` shows tracked paths (not empty)
- [ ] `git status --porcelain web/` has no `??` lines
- [ ] `git ls-files` does not include root `Hero-background.png`
- [ ] `git ls-files 'web/node_modules/*' 'web/.next/*'` is empty
</verification>

<success_criteria>
- PROD-02 satisfied: `web/` app source is tracked in git
- D-01 satisfied: full `web/public` included (including prune candidates present on disk)
- D-02 satisfied: commit contains only `web/` paths
- ROADMAP Phase 1 success criteria 1–3 all CLI-verified
- No `.env`, `node_modules`, or `.next` content in the git index under `web/`
</success_criteria>

<output>
After completion, create `.planning/phases/01-track-web-app/01-01-SUMMARY.md`
</output>
