---
phase: 07-archive-mockups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-archive-mockups/scripts/archive-verify.mjs
  - .planning/phases/07-archive-mockups/07-ARCHIVE.md
  - .planning/phases/07-archive-mockups/07-01-SUMMARY.md
autonomous: true
requirements:
  - MOCK-01
user_setup: []

must_haves:
  truths:
    - "Local branch refs/heads/archive/mockups exists with multi-commit mockups-only history (rev-list count ≥ 2)"
    - "Split tree has exactly 139 files whose relative paths match git ls-files mockups with mockups/ stripped"
    - "Sibling working-tree repo exists at the locked absolute path with HEAD equal to archive/mockups tip"
    - "Production current branch still tracks 139 mockups/ paths — mockups/ was never deleted"
    - "07-ARCHIVE.md records full tip SHA, sibling path, push-later one-liners, and hifi-ui-mockup prior-art note (not SoT)"
    - "No git push to origin was executed; no web/ app files were modified for archive"
  artifacts:
    - path: ".planning/phases/07-archive-mockups/scripts/archive-verify.mjs"
      provides: "Nyquist MOCK-01 automated gate (branch, history, file-set, sibling, main intact, ARCHIVE.md)"
      min_lines: 80
      contains: "archive/mockups"
    - path: ".planning/phases/07-archive-mockups/07-ARCHIVE.md"
      provides: "Phase 8 SoT record — tip SHA, sibling path, restore + push-later commands, prior art"
      min_lines: 40
      contains: "archive/mockups"
    - path: ".planning/phases/07-archive-mockups/07-01-SUMMARY.md"
      provides: "Execution proof with MOCK-01 evidence and no-push attestation"
      min_lines: 25
      contains: "MOCK-01"
  key_links:
    - from: "git subtree split --prefix=mockups"
      to: "refs/heads/archive/mockups"
      via: "create/re-create local branch at execute time"
      pattern: "subtree split"
    - from: "archive/mockups tip"
      to: "Dominion-Markets-mockups-archive HEAD"
      via: "git clone --no-hardlinks --branch archive/mockups"
      pattern: "no-hardlinks"
    - from: "07-ARCHIVE.md"
      to: "git rev-parse archive/mockups"
      via: "recorded full tip SHA must match"
      pattern: "tip"
    - from: "archive-verify.mjs"
      to: "main mockups/ + sibling + 07-ARCHIVE.md"
      via: "exit 0 asserts"
      pattern: "archive-verify"
---

<objective>
Satisfy **MOCK-01**: preserve full `mockups/` git history on local branch `archive/mockups`, materialize the locked sibling working-tree repo, and record coordinates in `07-ARCHIVE.md` — without deleting `mockups/` or pushing to `origin`.

Purpose: Phase 8/9 need a durable, findable archive before docs retarget and tree removal.
Output: `scripts/archive-verify.mjs` (exit 0), local `archive/mockups`, sibling repo at locked path, `07-ARCHIVE.md`, `07-01-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-archive-mockups/07-CONTEXT.md
@.planning/phases/07-archive-mockups/07-RESEARCH.md
@.planning/phases/07-archive-mockups/07-VALIDATION.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Locked paths and git contracts — use directly; do not invent alternate locations -->

Locked sibling path (D-02) — always quote in shell:
```text
/Users/usama/Desktop/Dominion Markets/Dominion-Markets-mockups-archive
```

Production repo (spaces) — always quote:
```text
/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs
```

Primary split (D-01):
```bash
git subtree split --prefix=mockups -b archive/mockups
```

Sibling materialize (D-02, working tree, --no-hardlinks):
```bash
git clone --no-hardlinks --single-branch --branch archive/mockups \
  "$REPO" "$SIBLING"
git -C "$SIBLING" branch -m main
```

Verify script entry (repo-relative; run from repo root):
```bash
node .planning/phases/07-archive-mockups/scripts/archive-verify.mjs
# Mid-plan after Task 2 only:
node .planning/phases/07-archive-mockups/scripts/archive-verify.mjs --allow-missing-sibling --allow-missing-archive-md
```

Expected counts (research-verified; gate on these, not commit-count equality to main's 64):
- Tracked `mockups/` on production branch: **139**
- Files on `archive/mockups` tree: **139**
- `git rev-list --count archive/mockups` ≥ **2** (dry-run observed 66)

Prior art (D-04 — note only, not SoT):
- `origin/hifi-ui-mockup` @ `184968a`
- tag `mockup-v0.1` @ `0bdf12c`

Hard stops: never `git push` to `origin`; never `git rm` / delete `mockups/`; never modify `web/` for this phase; do not retarget skills/READMEs (Phase 8).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wave 0 — author archive-verify.mjs</name>
  <files>.planning/phases/07-archive-mockups/scripts/archive-verify.mjs</files>
  <action>
Create `.planning/phases/07-archive-mockups/scripts/archive-verify.mjs` (Node ESM, shebang optional). Mirror Phase 6 pattern: resolve `REPO_ROOT` from `import.meta.url` via `path.resolve(__dirname, "../../../..")` so the script is **repo-relative** and portable for repo-rooted checks.

**Constants (locked):**
- Branch: `archive/mockups`
- Expected file count: `139`
- Sibling absolute path: `/Users/usama/Desktop/Dominion Markets/Dominion-Markets-mockups-archive` (CONTEXT D-02 — this absolute path is required; do not invent alternatives)
- Archive record: `.planning/phases/07-archive-mockups/07-ARCHIVE.md` under `REPO_ROOT`

**CLI flags (for progressive Nyquist sampling):**
- `--allow-missing-sibling` — skip sibling asserts (Task 2 mid-gate)
- `--allow-missing-archive-md` — skip `07-ARCHIVE.md` asserts (Task 2 mid-gate)
- Default (no flags): **all** asserts required

**Asserts (exit 0 only if all applicable pass):**
1. `refs/heads/archive/mockups` resolves; print full tip SHA
2. `git rev-list --count archive/mockups` ≥ 2
3. `git ls-tree -r --name-only archive/mockups` count === 139
4. Sorted relative paths on archive tip === sorted `(git ls-files mockups | sed 's|^mockups/||')`
5. On **current** branch (must not be `archive/mockups` for production checks): `git ls-files mockups` count === 139
6. Unless `--allow-missing-sibling`: sibling path is a git repo; `git -C sibling rev-parse HEAD` === archive tip
7. Unless `--allow-missing-archive-md`: `07-ARCHIVE.md` exists and contains (a) the full tip SHA, (b) the sibling absolute path string, (c) a push-later one-liner mentioning `origin` and `archive/mockups`

**Security (T-07-01):** Do not accept arbitrary user path argv for roots. Reject any constructed path containing `..`. Sibling path is a fixed constant. Quote-safe when spawning git (`execFile` / argv arrays preferred over shell strings).

**Do not** add Vitest/Playwright. **Do not** touch `web/` or `mockups/` contents.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-archive-mockups/scripts/archive-verify.mjs && node --check .planning/phases/07-archive-mockups/scripts/archive-verify.mjs && grep -v '^#' .planning/phases/07-archive-mockups/scripts/archive-verify.mjs | grep -c 'archive/mockups' | awk '{exit ($1<1)?1:0}' && grep -v '^#' .planning/phases/07-archive-mockups/scripts/archive-verify.mjs | grep -c 'allow-missing-sibling' | awk '{exit ($1<1)?1:0}'</automated>
  </verify>
  <done>Script exists, `node --check` passes, encodes branch/sibling/ARCHIVE asserts and progressive flags.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preflight + fresh subtree split to archive/mockups</name>
  <files>.planning/phases/07-archive-mockups/scripts/archive-verify.mjs</files>
  <action>
Run from the production repo root with **all paths quoted**. Satisfy D-01.

**Preflight:**
1. `test -d mockups`
2. `MAIN_COUNT=$(git ls-files mockups | wc -l | tr -d ' '); test "$MAIN_COUNT" = "139"`
3. Record current branch (`git rev-parse --abbrev-ref HEAD`) — expect `main` (document if different; do not switch to `archive/mockups` as working branch for the rest of the phase)
4. Do **not** `git add` untracked `mockups/.DS_Store`; dirty non-mockups files (e.g. `readme.md`) do not block split — leave them alone
5. Confirm disk space is adequate (sibling needs ≪ 1 GiB)

**Existing `archive/mockups` / sibling handling (prefer freshness — always re-split at execute time):**
- If `refs/heads/archive/mockups` already exists (e.g. research dry-run with `-b`, or partial prior execute): **delete the local branch** with `git branch -D archive/mockups` then re-run split. Do **not** reuse a stale tip as final SoT without re-split. Local-only delete is allowed; never delete remote refs; never `git push`.
- If sibling directory already exists at the locked path from a partial run: `rm -rf` that sibling path **only** (after confirming the path string equals the locked D-02 constant exactly). Re-create in Task 3. Do not touch production `mockups/`.
- Document in SUMMARY that re-split was chosen for freshness (even if branch was absent — state “fresh split”).

**Split (primary, D-01):**
```bash
git subtree split --prefix=mockups -b archive/mockups
```
Expect ~90–120s. Capture `TIP=$(git rev-parse archive/mockups)`.

**Immediate checks (before sibling):**
- `git rev-list --count archive/mockups` ≥ 2
- file count 139; `diff` empty between stripped main `mockups/` paths and archive tree paths
- Sample: `git log -1 --oneline archive/mockups`; `git blame -L 1,3 archive/mockups -- README.md` succeeds
- Confirm `git ls-files mockups | wc -l` still 139 on current production branch

**Fallback (Claude discretion, only if subtree fails):** `git-filter-repo --subdirectory-filter mockups` on a **disposable mirror clone** only; fetch tip into `archive/mockups` on this repo — never rewrite production `main` in-place. Prefer installing filter-repo only if needed.

**NEVER:** `git push` / `git subtree push` to `origin`; `git rm -r mockups`; edits under `web/`.

Run mid-gate:
`node .planning/phases/07-archive-mockups/scripts/archive-verify.mjs --allow-missing-sibling --allow-missing-archive-md`
  </action>
  <verify>
    <automated>node .planning/phases/07-archive-mockups/scripts/archive-verify.mjs --allow-missing-sibling --allow-missing-archive-md</automated>
  </verify>
  <done>Local archive/mockups exists from execute-time split; history ≥2 commits; 139-file set matches main mockups/ relatives; production mockups/ still 139; mid-gate script exit 0; no origin push.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sibling repo + 07-ARCHIVE.md + full verify + SUMMARY</name>
  <files>.planning/phases/07-archive-mockups/07-ARCHIVE.md, .planning/phases/07-archive-mockups/07-01-SUMMARY.md</files>
  <action>
Satisfy D-02, D-03, D-04. Quote all paths with spaces.

**Sibling (D-02):**
```bash
REPO="$(git rev-parse --show-toplevel)"
SIBLING="/Users/usama/Desktop/Dominion Markets/Dominion-Markets-mockups-archive"
# If path exists from partial run, remove only after exact-path check, then:
git clone --no-hardlinks --single-branch --branch archive/mockups "$REPO" "$SIBLING"
git -C "$SIBLING" branch -m main
test "$(git -C "$SIBLING" rev-parse HEAD)" = "$(git rev-parse archive/mockups)"
```

**Sibling remote policy (RESEARCH OQ1 RESOLVED):** Keep clone `origin` pointing at the production local path (documents provenance). Optionally rename remote to `production-local` — either is fine; document which was chosen in sibling README and `07-ARCHIVE.md`. Do **not** add a GitHub remote.

**Sibling README (discretion):** Commit **inside the sibling repo only** a short provenance README (source repo path, branch `archive/mockups`, tip SHA, date, note that production `mockups/` remains until Phase 9). Do not commit sibling files into the production repo.

**Write `07-ARCHIVE.md`** at `.planning/phases/07-archive-mockups/07-ARCHIVE.md` with required sections (per RESEARCH):
1. Status — MOCK-01 archive complete (local)
2. Source of truth — `archive/mockups` + sibling path (D-04 primary SoT)
3. Coordinates — production repo path, branch, **full tip SHA**, split method + date, commit count, file count 139
4. Sibling — absolute path, HEAD SHA, branch renamed to `main` yes/no, remote policy
5. Verification — point to `archive-verify.mjs` exit 0
6. Prior art (**not SoT**, D-04) — `origin/hifi-ui-mockup` @ `184968a`; tag `mockup-v0.1` @ `0bdf12c`
7. Hard stops observed — `mockups/` still on main (139); no `web/` changes; **no** `git push` to `origin`
8. Restore — how to clone/checkout from branch or sibling
9. Push later (**do not run now**, D-03) — exact one-liners, e.g. `git push -u origin archive/mockups` and optional future archive remote
10. Next — Phase 8 retarget docs to this file; Phase 9 delete `mockups/`

**Full gate:** `node .planning/phases/07-archive-mockups/scripts/archive-verify.mjs` (no allow flags) must exit 0.

**Write `07-01-SUMMARY.md`:** MOCK-01 evidence (tip SHA, counts, sibling path, verify exit 0), attestation that no `git push` to `origin` occurred, re-split freshness note, no `web/` / no `mockups/` deletion. Do **not** mark ROADMAP Phase 7 complete (orchestrator owns).

Commit **production-repo** planning artifacts only (`scripts/`, `07-ARCHIVE.md`, SUMMARY, any RESEARCH touch already done by planner). The sibling is outside this repo.
  </action>
  <verify>
    <automated>node .planning/phases/07-archive-mockups/scripts/archive-verify.mjs && test -f .planning/phases/07-archive-mockups/07-ARCHIVE.md && grep -v '^#' .planning/phases/07-archive-mockups/07-ARCHIVE.md | grep -c 'hifi-ui-mockup' | awk '{exit ($1<1)?1:0}' && grep -v '^#' .planning/phases/07-archive-mockups/07-ARCHIVE.md | grep -c 'git push -u origin archive/mockups' | awk '{exit ($1<1)?1:0}' && test -f .planning/phases/07-archive-mockups/07-01-SUMMARY.md && grep -v '^#' .planning/phases/07-archive-mockups/07-01-SUMMARY.md | grep -c 'MOCK-01' | awk '{exit ($1<1)?1:0}'</automated>
  </verify>
  <done>Sibling HEAD matches archive tip; 07-ARCHIVE.md has tip SHA, sibling path, push-later cmds, hifi prior-art; full archive-verify exit 0; SUMMARY attests no origin push and mockups/ retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor shell → local git object DB | Split/clone mutate refs and filesystem; must not touch remotes |
| Verify script → fixed roots | Script reads git + sibling path; must not accept traversal / arbitrary roots |
| Planning docs → future humans/Phase 8 | ARCHIVE.md is SoT pointer; must not embed secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | `scripts/archive-verify.mjs` | mitigate | Fixed sibling constant; reject `..` in path joins; prefer `execFile` argv over shell interpolation |
| T-07-02 | E / I | `git push` / wrong remote | mitigate | D-03: no push in tasks; SUMMARY attestation; verify does not push; push-later cmds are docs only |
| T-07-03 | T | Production `main` / `mockups/` | mitigate | Subtree split non-destructive; verify asserts 139 `mockups/` paths remain; never `git rm mockups` |
| T-07-04 | T | filter-repo fallback | mitigate | Only on disposable mirror clone; never rewrite this working tree's `main` |
| T-07-05 | I | `07-ARCHIVE.md` / sibling README | accept | Mockups are design assets (no `.env` in tracked set per research); do not add untracked junk; no secrets in ARCHIVE |
</threat_model>

<verification>
- Mid: `node .planning/phases/07-archive-mockups/scripts/archive-verify.mjs --allow-missing-sibling --allow-missing-archive-md`
- Final: `node .planning/phases/07-archive-mockups/scripts/archive-verify.mjs`
- Confirm `git ls-files mockups | wc -l` is 139 on production branch after all tasks
- Confirm `git status -sb` shows no new `origin/archive/mockups` remote-tracking from a push
</verification>

<success_criteria>
- MOCK-01 satisfied locally: `archive/mockups` + sibling repo + `07-ARCHIVE.md`
- History preserved (multi-commit; 139-file set match)
- `mockups/` still in production tree; no `web/` archive-driven edits; no `git push` to `origin`
- `archive-verify.mjs` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-archive-mockups/07-01-SUMMARY.md`
</output>
