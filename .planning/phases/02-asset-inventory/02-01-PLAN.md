---
phase: 02-asset-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-asset-inventory/02-INVENTORY.md
  - .planning/phases/02-asset-inventory/02-INVENTORY.json
  - .planning/phases/02-asset-inventory/02-01-SUMMARY.md
autonomous: true
requirements:
  - ASSET-01
user_setup: []

must_haves:
  truths:
    - "Every file under web/public (including assets/ and mockups.css) appears in both inventories with class keep or delete"
    - "Delete candidates list path + size; keep list includes referenced hero PNG, logo paths, and body media from home-html.ts / served CSS"
    - "Classification evidence cites web/src and/or web/public/mockups.css — never mockups/ or docs/ as keep proof"
    - "No files under web/public were deleted in this phase"
  artifacts:
    - path: ".planning/phases/02-asset-inventory/02-INVENTORY.md"
      provides: "Human-readable keep/delete inventory with path, size, class, evidence"
      min_lines: 40
      contains: "class"
    - path: ".planning/phases/02-asset-inventory/02-INVENTORY.json"
      provides: "Machine-readable inventory for Phase 5 deletes"
      contains: "\"class\""
    - path: ".planning/phases/02-asset-inventory/02-01-SUMMARY.md"
      provides: "Execution record + CONCERNS cross-check notes"
      min_lines: 20
  key_links:
    - from: "web/src + web/public/mockups.css"
      to: "02-INVENTORY.json keep entries"
      via: "extracted path tokens matched to on-disk public files"
      pattern: "\"class\":\\s*\"keep\""
    - from: "find web/public -type f"
      to: "02-INVENTORY.md + 02-INVENTORY.json"
      via: "every on-disk public file classified exactly once"
      pattern: "web/public/"
    - from: ".planning/codebase/CONCERNS.md candidate table"
      to: "02-01-SUMMARY.md"
      via: "cross-check deltas only (not sole authority)"
      pattern: "CONCERNS"
---

<objective>
Produce a complete, static-scan-backed keep vs delete inventory for every file under `web/public` (ASSET-01), delivered as both Markdown and JSON (D-01, D-02, D-03).

Purpose: Phase 5 must delete by list, not guesswork; humans need a reviewable keep/delete table with sizes and evidence.
Output: `02-INVENTORY.md`, `02-INVENTORY.json`, and `02-01-SUMMARY.md` (with CONCERNS cross-check). No deletes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-asset-inventory/02-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01-track-web-app/01-01-SUMMARY.md

@web/src/content/home-html.ts
@web/src/components/CinematicHero.tsx
@web/src/app/layout.tsx
@web/src/lib/mockups-main.js
@web/public/mockups.css

## Locked decisions (NON-NEGOTIABLE)

- **D-01:** Deliver both Markdown + JSON inventories.
- **D-02:** Paths must be exactly:
  - `.planning/phases/02-asset-inventory/02-INVENTORY.md`
  - `.planning/phases/02-asset-inventory/02-INVENTORY.json`
- **D-03:** Every entry: `path`, size (`bytes` and/or human), `class` (`keep`|`delete`), `evidence`.

## Claude's Discretion (apply as written)

- **Fresh rescan** of on-disk `web/public` vs references extracted from all of `web/src` (including `home-html.ts` string content and `mockups-main.js`) **and** `web/public/mockups.css` (`url(...)` / path strings).
- Do **not** use `mockups/` or `docs/` as keep evidence for production public files.
- Use `.planning/codebase/CONCERNS.md` **only** as a sanity cross-check after the fresh scan — not as the sole authority.
- Comment/docstring-only mentions → **keep**. Unresolved dynamic/constructed paths → **keep** with note.
- **No deletes** this phase (inventory artifacts only).

## Known live keep anchors (must appear as keep if files exist)

- `/assets/hero/Hero-background.png` — `CinematicHero.tsx`
- `/assets/logo/logo-horizontal-reverse.svg` — `CinematicHero.tsx`
- `/assets/logo/logo-isotype-app.png` — `layout.tsx` icons
- `/mockups.css` — `layout.tsx` stylesheet link
- Body `/assets/...` paths inside `home-html.ts` and `url(.../assets/...)` in `web/public/mockups.css`
- Platform badges in `mockups-main.js` (e.g. `mt5-official.png`, `ctrader-official.png`)

## Out of inventory scope

- Repo-root `Hero-background.png` — Phase 3
- `mockups/` tree — Phases 7–9
- Actual file deletion — Phase 5 (and Phase 3 for non-public orphans)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract static asset references from scan sources</name>
  <files>.planning/phases/02-asset-inventory/.refs-extracted.txt</files>
  <read_first>.planning/phases/02-asset-inventory/02-CONTEXT.md, web/src/content/home-html.ts, web/src/components/CinematicHero.tsx, web/src/app/layout.tsx, web/src/lib/mockups-main.js, web/public/mockups.css</read_first>
  <action>
Per D-03 / Claude discretion: perform a **fresh** extract of every public-path token referenced by scan sources. Do not trust CONCERNS.md numbers yet.

1. Scan **only** these sources (no `mockups/`, no `docs/`):
   - All files under `web/src/` (any text/source file; must include `home-html.ts` string content and `mockups-main.js`)
   - `web/public/mockups.css` (including `url(...)` values; strip `?query` / hash suffixes)

2. Extract tokens that map to files under `web/public`, including at least:
   - Absolute site paths: `/assets/...`, `/mockups.css`
   - `url("/assets/...")` / `url('/assets/...')` / `url(/assets/...)`
   - Bare `assets/...` only when clearly a public path (normalize to `/assets/...`)

3. Normalize each hit to a repo-relative public path:
   - `/assets/foo.png` → `web/public/assets/foo.png`
   - `/mockups.css` → `web/public/mockups.css`
   - Drop query strings (`?v=110`) before matching disk

4. Write a working file `.planning/phases/02-asset-inventory/.refs-extracted.txt` — one normalized `web/public/...` path per line, sorted unique. Optionally append `# evidence: relative-source:approx` comments on separate lines starting with `#` (comments must not break uniqueness of path lines).

5. Do **not** delete or modify any file under `web/public` or `web/src`.
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-asset-inventory/.refs-extracted.txt" && grep -E '^web/public/' ".planning/phases/02-asset-inventory/.refs-extracted.txt" | wc -l | awk '$1>=20{exit 0}{exit 1}' && grep -F 'web/public/assets/hero/Hero-background.png' ".planning/phases/02-asset-inventory/.refs-extracted.txt" && grep -F 'web/public/mockups.css' ".planning/phases/02-asset-inventory/.refs-extracted.txt" && ! grep -E '^mockups/' ".planning/phases/02-asset-inventory/.refs-extracted.txt"</automated>
  </verify>
  <acceptance_criteria>
    - `.refs-extracted.txt` exists under `.planning/phases/02-asset-inventory/`
    - File contains line `web/public/assets/hero/Hero-background.png`
    - File contains line `web/public/mockups.css`
    - File contains line `web/public/assets/logo/logo-isotype-app.png` OR `web/public/assets/logo/logo-horizontal-reverse.svg`
    - `grep -c '^mockups/' .refs-extracted.txt` is `0` (no mockups/ tree paths as refs)
    - Path lines are unique: `grep -E '^web/public/' .refs-extracted.txt | sort | uniq -d` prints nothing
  </acceptance_criteria>
  <done>Unique normalized public paths extracted from web/src + web/public/mockups.css only; hero + served CSS present; no mockups/ keep evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Diff on-disk web/public vs refs and classify keep|delete</name>
  <files>.planning/phases/02-asset-inventory/.classification.jsonl</files>
  <read_first>.planning/phases/02-asset-inventory/.refs-extracted.txt, .planning/phases/02-asset-inventory/02-CONTEXT.md</read_first>
  <action>
1. Enumerate **every** on-disk file: `find web/public -type f` (include `.DS_Store`, `mockups.css`, all under `assets/`). Record byte size per file (`stat` / `wc -c` / `find -exec stat`).

2. Classify each on-disk file (per D-03 + ambiguity rules):
   - **keep** if its normalized path appears in `.refs-extracted.txt`, OR if the only hits are comment/docstring-only (still keep), OR if a dynamic/constructed path cannot be resolved statically (keep + note in evidence).
   - **delete** if no match in scan sources after normalization (evidence: `no match in web/src or web/public/mockups.css`).
   - `.DS_Store` → **delete** with evidence `OS junk; not a runtime asset` (still inventory it — do not delete the file from disk).

3. Evidence string rules:
   - keep: cite at least one source file + path token (e.g. `CinematicHero.tsx:/assets/hero/Hero-background.png` or `mockups.css:url(/assets/footer/...)`).
   - delete: exact phrase starting with `no match` or `OS junk`.

4. Write working `.planning/phases/02-asset-inventory/.classification.jsonl` — one JSON object per line with keys: `path`, `bytes`, `class`, `evidence`. Every `find web/public -type f` path must appear exactly once. Do not omit files that CONCERNS.md never listed.

5. Still **no disk deletes** under `web/public`.
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-asset-inventory/.classification.jsonl" && DISK=$(find web/public -type f | wc -l | tr -d ' ') && LINES=$(grep -c '"path"' ".planning/phases/02-asset-inventory/.classification.jsonl") && test "$DISK" -eq "$LINES" && grep -F '"class":"keep"' ".planning/phases/02-asset-inventory/.classification.jsonl" | head -1 && grep -F '"class":"delete"' ".planning/phases/02-asset-inventory/.classification.jsonl" | head -1 && grep -F 'Hero-background.png' ".planning/phases/02-asset-inventory/.classification.jsonl" | grep -F '"class":"keep"'</automated>
  </verify>
  <acceptance_criteria>
    - Line count of `.classification.jsonl` equals `find web/public -type f | wc -l`
    - Every line has keys path, bytes, class, evidence
    - `Hero-background.png` row has `"class":"keep"`
    - At least one `"class":"delete"` row exists (expected orphans such as `hero.mp4` if still unreferenced)
    - No file under `web/public` was removed: `test -f web/public/mockups.css` and `test -d web/public/assets`
  </acceptance_criteria>
  <done>Complete 1:1 classification of every on-disk web/public file with sizes and evidence; keep-on-ambiguity applied; disk untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Write dual inventories, CONCERNS cross-check, and SUMMARY</name>
  <files>.planning/phases/02-asset-inventory/02-INVENTORY.md, .planning/phases/02-asset-inventory/02-INVENTORY.json, .planning/phases/02-asset-inventory/02-01-SUMMARY.md</files>
  <read_first>.planning/phases/02-asset-inventory/.classification.jsonl, .planning/phases/02-asset-inventory/02-CONTEXT.md, .planning/codebase/CONCERNS.md, .planning/REQUIREMENTS.md</read_first>
  <action>
Per **D-01 / D-02 / D-03** and ASSET-01:

### A) Write `02-INVENTORY.json`
Stable schema (Phase 5 consumer):
```json
{
  "generated": "<ISO-8601 date>",
  "phase": "02-asset-inventory",
  "requirement": "ASSET-01",
  "scan_sources": ["web/src", "web/public/mockups.css"],
  "summary": { "total": N, "keep": K, "delete": D, "delete_bytes": B },
  "files": [
    { "path": "web/public/...", "bytes": 123, "class": "keep"|"delete", "evidence": "..." }
  ]
}
```
- `files` must include every entry from `.classification.jsonl` (same count as `find web/public -type f`).
- Sort `files` by `path` ascending for stable diffs.

### B) Write `02-INVENTORY.md`
Human-readable mirror with:
1. Title + scan method (sources, ambiguity rules, "no deletes this phase")
2. Summary counts (total / keep / delete / delete_bytes human + raw)
3. **Keep** table: path | size | evidence
4. **Delete** table: path | size | evidence (must include byte sizes — success criterion)
5. Explicit callout that these known live assets are **keep** (if present on disk): `Hero-background.png`, logo reverse SVG, logo isotype PNG, body media referenced from `home-html.ts`, served `mockups.css`

### C) CONCERNS.md cross-check (discretion — not sole authority)
Compare fresh delete set vs CONCERNS.md "Candidate unreferenced paths" table (~53 / ~32.6MB). In `02-01-SUMMARY.md` document:
- Overlap count
- In CONCERNS but now **keep** (with why — e.g. newly found ref)
- In fresh delete but not in CONCERNS (new finds)
- Do **not** reclassify solely to match CONCERNS; fresh scan wins

### D) Write `02-01-SUMMARY.md`
Record commands used, totals, largest delete candidates (expect `hero.mp4` if delete), CONCERNS deltas, confirmation that **zero** `web/public` files were deleted, and ASSET-01 satisfied.

### E) Cleanup (optional)
May delete working `.refs-extracted.txt` / `.classification.jsonl` **or** leave them untracked — do not commit junk if committing inventories; prefer leaving them out of git add unless useful. Canonical deliverables are the two `02-INVENTORY.*` files + SUMMARY.
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-asset-inventory/02-INVENTORY.md" && test -f ".planning/phases/02-asset-inventory/02-INVENTORY.json" && python3 -c "import json; d=json.load(open('.planning/phases/02-asset-inventory/02-INVENTORY.json')); assert d.get('requirement')=='ASSET-01'; assert all(set(x)>={'path','bytes','class','evidence'} for x in d['files']); assert all(x['class'] in ('keep','delete') for x in d['files']); import subprocess; n=int(subprocess.check_output('find web/public -type f | wc -l',shell=True)); assert len(d['files'])==n, (len(d['files']),n); keep=next(x for x in d['files'] if x['path'].endswith('Hero-background.png')); assert keep['class']=='keep'" && grep -E 'keep|delete' ".planning/phases/02-asset-inventory/02-INVENTORY.md" | head -5 && grep -F 'CONCERNS' ".planning/phases/02-asset-inventory/02-01-SUMMARY.md" && test -f web/public/assets/hero/hero.mp4</automated>
  </verify>
  <acceptance_criteria>
    - Both D-02 paths exist: `02-INVENTORY.md` and `02-INVENTORY.json`
    - JSON `files.length` equals `find web/public -type f | wc -l`
    - Every JSON object has path, bytes, class, evidence (D-03)
    - MD delete section lists sizes (grep for byte numbers or `KB`/`MB` next to delete paths)
    - JSON has `"class":"keep"` for `web/public/assets/hero/Hero-background.png`
    - JSON has keep entries whose paths match logo and/or home-html body assets
    - `02-01-SUMMARY.md` mentions CONCERNS cross-check
    - `hero.mp4` still exists on disk if it did before (no deletes): `test -f web/public/assets/hero/hero.mp4` OR document absence if already missing
    - Neither inventory cites `mockups/` as keep evidence for a public file
  </acceptance_criteria>
  <done>Dual inventories published at canonical paths; every public file classified; CONCERNS cross-checked in SUMMARY; ASSET-01 satisfied; disk unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → filesystem (`web/public`, `web/src`) | Read-only inventory of local tree; no network, no user input |
| Inventory artifacts → Phase 5 delete automation | Downstream phases may delete whatever is marked `delete` — misclassification is the primary risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `02-INVENTORY.json` class field | mitigate | Require non-empty `evidence` per entry (D-03); keep-on-ambiguity / comment-only → keep; CONCERNS is cross-check only |
| T-02-02 | Information Disclosure | Inventory listing asset paths/sizes | accept | Local planning docs; no secrets; paths already in repo |
| T-02-03 | Denial of Service | Accidental mass delete during inventory | mitigate | Plan forbids any `rm`/`git rm` under `web/public`; verify files still on disk after write |
| T-02-04 | Elevation / Spoofing | N/A — no auth surface | accept | Read-only docs phase; no trust boundary to attackers |
| T-02-05 | Repudiation | Scan method undocumented | mitigate | MD + JSON record `scan_sources` and evidence strings for audit |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `find web/public -type f | wc -l` equals JSON `files.length`
- [ ] `02-INVENTORY.md` and `02-INVENTORY.json` both exist at D-02 paths
- [ ] Every entry has path, size/bytes, class, evidence
- [ ] Keep list includes Hero-background.png + logo/favicon paths + body media refs
- [ ] Delete list includes sizes; known orphans (e.g. hero.mp4) appear if unreferenced
- [ ] No `web/public` files deleted
- [ ] SUMMARY documents CONCERNS cross-check deltas
</verification>

<success_criteria>
- ASSET-01 satisfied: inventory of `web/public` against `web/src` + served `web/public/mockups.css`
- D-01/D-02/D-03 satisfied (dual artifacts, canonical paths, full entry schema)
- Operators can hand Phase 5 a delete list with sizes without re-scanning
- Zero production assets removed in this phase
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-inventory/02-01-SUMMARY.md`
</output>
