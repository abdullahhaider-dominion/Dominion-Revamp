---
phase: 04-minimal-react-unlock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-minimal-react-unlock/04-UNLOCK.md
  - .planning/phases/04-minimal-react-unlock/04-01-SUMMARY.md
autonomous: true
requirements:
  - REACT-01
user_setup: []

must_haves:
  truths:
    - "Every Phase 2 inventory delete-class path was re-scanned against web/src and web/public/mockups.css"
    - "If all delete candidates remain unreferenced, no React/HTML/CSS caller files were modified"
    - "Any delete candidate found referenced is listed as a keep-exception with an explicit written reason (not silently skipped)"
    - "04-UNLOCK.md states whether Phase 5 may delete the full remaining still-unreferenced delete list"
    - "No public asset files were deleted in this phase"
  artifacts:
    - path: ".planning/phases/04-minimal-react-unlock/04-UNLOCK.md"
      provides: "Verify-only unlock report for Phase 5 prune gate"
      min_lines: 40
      contains: "Phase 5"
    - path: ".planning/phases/04-minimal-react-unlock/04-01-SUMMARY.md"
      provides: "Execution summary with rescan counts and no-app-change proof"
      min_lines: 20
  key_links:
    - from: ".planning/phases/04-minimal-react-unlock/04-UNLOCK.md"
      to: ".planning/phases/02-asset-inventory/02-INVENTORY.json"
      via: "candidate set = files where class=delete"
      pattern: "02-INVENTORY\\.json"
    - from: ".planning/phases/04-minimal-react-unlock/04-UNLOCK.md"
      to: "web/src"
      via: "rescan method documents ripgrep/static match against web/src"
      pattern: "web/src"
    - from: ".planning/phases/04-minimal-react-unlock/04-UNLOCK.md"
      to: "web/public/mockups.css"
      via: "rescan method documents match against served mockups.css"
      pattern: "mockups\\.css"
---

<objective>
Satisfy REACT-01 via a verify-only unlock: reconfirm every Phase 2 `delete` candidate is still unreferenced in `web/src` + `web/public/mockups.css`, then write `04-UNLOCK.md` so Phase 5 can prune safely — without React/HTML changes unless a conflict forces a keep-exception (per D-01/D-02).

Purpose: Close the Phase 4 gate between inventory and public prune without risky caller edits. Phase 2 already marked 55 paths unmatched; this phase proves that still holds (or documents keep-exceptions).
Output: `04-UNLOCK.md` + `04-01-SUMMARY.md`. No app code changes expected. No asset deletes (Phase 5).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-minimal-react-unlock/04-CONTEXT.md
@.planning/phases/02-asset-inventory/02-INVENTORY.json
@.planning/phases/02-asset-inventory/02-CONTEXT.md

@web/src/content/home-html.ts
@web/src/components/CinematicHero.tsx
@web/public/mockups.css

## Locked decisions (NON-NEGOTIABLE)

- **D-01:** Verify-only — if the delete list is still fully unreferenced, **do not** change React/HTML/CSS callers.
- **D-02:** If a delete-listed file is found referenced → **keep** it and write an explicit reason in `04-UNLOCK.md`. Do **not** force a caller edit in this phase.
- **D-03:** Write `.planning/phases/04-minimal-react-unlock/04-UNLOCK.md` with rescan method, still-unreferenced count, keep-exceptions, and Phase 5 delete clearance statement.

## Deferred (MUST NOT implement)

- Actual `web/public` asset deletion → Phase 5
- Full `home-html.ts` React rewrite → v2 / out of scope

## Claude's Discretion

- Candidate set = only `class: "delete"` rows from `02-INVENTORY.json` (expected **55**). Do not invent new deletes.
- Optional light cross-check of largest orphans (`hero.mp4`, mist-bg files) with ripgrep; still must cover **all** 55.
- Ambiguous / comment-only hits → treat as referenced → keep-exception (safer), same spirit as Phase 2.

## Exact scope — WRITE only these

1. `.planning/phases/04-minimal-react-unlock/04-UNLOCK.md`
2. `.planning/phases/04-minimal-react-unlock/04-01-SUMMARY.md`

## Exact denylist — NEVER modify/delete in this plan

- Any file under `web/src/` (unless D-02 somehow required a caller edit — it does **not**; keep instead)
- `web/public/mockups.css` content
- Any path under `web/public/` (no deletes)
- `mockups/` tree
- `02-INVENTORY.json` / `02-INVENTORY.md` (read-only source of truth for candidates)

## Inventory contract (from Phase 2)

```json
{
  "summary": { "total": 137, "keep": 82, "delete": 55, "delete_bytes": 34209521 },
  "scan_sources": ["web/src", "web/public/mockups.css"],
  "files": [{ "path": "web/public/...", "bytes": 0, "class": "delete|keep", "evidence": "..." }]
}
```

Largest delete candidates (optional spot-check): `web/public/assets/hero/hero.mp4`, mist-bg under blogs/awards/markets-reach/mrkt/mt5-sim.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load delete list and re-scan all candidates</name>
  <files>.planning/phases/04-minimal-react-unlock/04-UNLOCK.md</files>
  <read_first>.planning/phases/04-minimal-react-unlock/04-CONTEXT.md, .planning/phases/02-asset-inventory/02-INVENTORY.json, .planning/REQUIREMENTS.md</read_first>
  <action>
Per **D-01** / REACT-01: treat this as verify-only.

1. Load candidates: parse `.planning/phases/02-asset-inventory/02-INVENTORY.json` and select every object in `files[]` where `class == "delete"`. Expect exactly **55** paths (do not invent extras; do not drop any).

2. For each delete path, derive searchable tokens the same way Phase 2 did:
   - Full public URL-ish path after `web/public` (e.g. `/assets/hero/hero.mp4`)
   - Path without leading slash
   - Basename (e.g. `hero.mp4`) — if basename alone matches, inspect hit context; comment/docstring-only or ambiguous → count as **referenced** (keep-exception per safer rule)

3. Re-scan **only** these sources (same as Phase 2 / CONTEXT):
   - Entire tree `web/src` (includes `home-html.ts` string HTML and `mockups-main.js` if present under src)
   - File `web/public/mockups.css`
   Do **not** use `mockups/`, `docs/`, or inventory evidence strings as keep proof.

4. Classify each candidate:
   - **still-unreferenced** — zero substantive matches in scan sources
   - **keep-exception** — one or more substantive matches; record file path of hit + short reason (per **D-02**: keep, do not edit callers)

5. Optional discretion spot-check: `rg` basename/`hero.mp4` and `mist-bg` across scan sources; results must agree with the full 55-path pass.

6. Do **not** write app code. Do **not** delete public files. Persist structured results for Task 2 (counts + exception rows).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('.planning/phases/02-asset-inventory/02-INVENTORY.json')); assert sum(1 for f in d['files'] if f['class']=='delete')==55"</automated>
  </verify>
  <acceptance_criteria>
    - Candidate count loaded from inventory equals 55 (summary.delete and count of class=delete rows)
    - Every one of the 55 paths was checked against both `web/src` and `web/public/mockups.css`
    - Result set partitions into still-unreferenced + keep-exceptions with no silent skips
    - No files under `web/src` or `web/public` were modified during this task (`git status --short web/` shows clean for those paths)
  </acceptance_criteria>
  <done>All 55 delete candidates rescanned; each is either still-unreferenced or a documented keep-exception candidate; zero app/asset mutations.</done>
</task>

<task type="auto">
  <name>Task 2: Write 04-UNLOCK.md unlock artifact</name>
  <files>.planning/phases/04-minimal-react-unlock/04-UNLOCK.md</files>
  <read_first>.planning/phases/04-minimal-react-unlock/04-CONTEXT.md, .planning/phases/02-asset-inventory/02-INVENTORY.json</read_first>
  <action>
Per **D-03**, create `.planning/phases/04-minimal-react-unlock/04-UNLOCK.md` with these sections (exact headings):

1. `# Phase 4: Minimal React Unlock`
2. `## Status` — one of: `VERIFY_ONLY_CLEAR` (all 55 still unreferenced, no caller edits) OR `KEEP_EXCEPTIONS` (one or more referenced)
3. `## Rescan method` — document: candidate source `02-INVENTORY.json` class=delete; scan sources `web/src` + `web/public/mockups.css`; token rules (public path + basename); date
4. `## Counts` — table or bullets: `candidates`, `still_unreferenced`, `keep_exceptions` (must sum: still_unreferenced + keep_exceptions == candidates)
5. `## Keep exceptions` — if none: explicit sentence `None — all delete candidates remain unreferenced.` If any: bullet list `path` + `reason` + `hit_location` (file:line or file path)
6. `## Phase 5 clearance` — **required clear statement**:
   - If zero keep-exceptions: `Phase 5 may delete all N still-unreferenced paths from the Phase 2 delete list.` (N = still_unreferenced count)
   - If keep-exceptions exist: `Phase 5 may delete the still-unreferenced subset only; must NOT delete keep-exception paths:` then list those paths
7. `## Caller changes` — state `None` when Status is VERIFY_ONLY_CLEAR (per **D-01**). Never claim caller edits that were not made.

Do not modify `web/` source. Do not delete assets.
  </action>
  <verify>
    <automated>test -f ".planning/phases/04-minimal-react-unlock/04-UNLOCK.md" && grep -q "## Phase 5 clearance" ".planning/phases/04-minimal-react-unlock/04-UNLOCK.md" && grep -q "02-INVENTORY.json" ".planning/phases/04-minimal-react-unlock/04-UNLOCK.md" && grep -q "web/src" ".planning/phases/04-minimal-react-unlock/04-UNLOCK.md" && grep -q "mockups.css" ".planning/phases/04-minimal-react-unlock/04-UNLOCK.md"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-minimal-react-unlock/04-UNLOCK.md` exists
    - Contains headings `## Rescan method`, `## Counts`, `## Keep exceptions`, `## Phase 5 clearance`, `## Caller changes`
    - Mentions `02-INVENTORY.json`, `web/src`, and `mockups.css`
    - `## Phase 5 clearance` contains the literal substring `Phase 5 may delete`
    - If Status is VERIFY_ONLY_CLEAR, `## Caller changes` contains `None` and `## Keep exceptions` states none remain referenced
  </acceptance_criteria>
  <done>04-UNLOCK.md records method, counts, exceptions, and Phase 5 clearance per D-03.</done>
</task>

<task type="auto">
  <name>Task 3: Write SUMMARY and prove no app code changes</name>
  <files>.planning/phases/04-minimal-react-unlock/04-01-SUMMARY.md</files>
  <read_first>.planning/phases/04-minimal-react-unlock/04-UNLOCK.md, .planning/phases/04-minimal-react-unlock/04-01-PLAN.md</read_first>
  <action>
Create `.planning/phases/04-minimal-react-unlock/04-01-SUMMARY.md` using the standard SUMMARY template fields:

- What was done: verify-only rescan of 55 delete candidates; unlock artifact written
- Decisions honored: D-01, D-02, D-03
- Key metrics from `04-UNLOCK.md` (candidates / still_unreferenced / keep_exceptions / Status)
- Explicit proof of **no app code changes**: paste output of `git status --short -- web/src web/public` showing empty (or only unrelated pre-existing dirt — if dirt exists, confirm none of this plan's tasks introduced diffs via `git diff --stat -- web/src web/public`)
- REACT-01 outcome: either "no caller change required" or "keep-exceptions documented instead of forced unlock"
- Next: Phase 5 may proceed only against the clearance statement in `04-UNLOCK.md`

Do not stage or commit `web/` files. Planning docs commit is handled by execute-plan / orchestrator norms for this phase's artifacts.
  </action>
  <verify>
    <automated>test -f ".planning/phases/04-minimal-react-unlock/04-01-SUMMARY.md" && test -z "$(git diff --stat -- web/src web/public)" && test -f web/public/assets/hero/hero.mp4</automated>
  </verify>
  <acceptance_criteria>
    - `04-01-SUMMARY.md` exists and references `04-UNLOCK.md`
    - SUMMARY states REACT-01 satisfied via verify-only (or keep-exceptions without caller edits)
    - `git diff --stat -- web/src web/public` is empty after plan execution
    - No `web/public` asset paths were deleted (spot-check: `test -f web/public/assets/hero/hero.mp4` still true if it was a delete candidate still on disk)
  </acceptance_criteria>
  <done>SUMMARY written; proof recorded that Phase 4 made no React/HTML/public mutations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inventory JSON → Phase 5 delete executor | Unlock report authorizes which paths may be deleted; false clearance deletes live assets |
| Static scan sources (`web/src`, `mockups.css`) → classification | Only these sources prove "unused"; other trees must not invent keep/delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `04-UNLOCK.md` Phase 5 clearance | mitigate | Require full 55-path rescan against `web/src` + `mockups.css`; clearance text must match still-unreferenced set; keep-exceptions listed by path |
| T-04-02 | Information Disclosure | Unlock/SUMMARY artifacts | accept | Paths/sizes are local marketing assets already in repo; no secrets in inventory |
| T-04-03 | Repudiation | Rescan method undocumented | mitigate | `04-UNLOCK.md` must include `## Rescan method` with sources, token rules, and date |
| T-04-04 | Denial of Service | Accidental delete in Phase 4 | mitigate | Denylist: no deletes under `web/public`; Task 3 verifies assets still on disk / empty `git diff` for web trees |
| T-04-05 | Elevation of Privilege | N/A — docs-only phase | accept | No auth, APIs, or privilege boundaries in scope |
| T-04-06 | Spoofing | Forged inventory candidates | mitigate | Candidates must be loaded from committed `02-INVENTORY.json` class=delete only — no invented paths |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `02-INVENTORY.json` delete count still 55
- [ ] `04-UNLOCK.md` exists with method, counts, exceptions, Phase 5 clearance
- [ ] `git diff --stat -- web/src web/public` empty
- [ ] No public assets deleted
- [ ] `04-01-SUMMARY.md` records Status and REACT-01 outcome
</verification>

<success_criteria>
- REACT-01 satisfied: either no caller change needed (all deletes still unreferenced) or every conflict is a written keep-exception (D-02) — no silent skips
- `04-UNLOCK.md` is the single gate artifact Phase 5 consumes for clearance
- Zero React/HTML changes when Status is VERIFY_ONLY_CLEAR (D-01)
- Phase does not delete any files under `web/public`
</success_criteria>

<output>
After completion, create `.planning/phases/04-minimal-react-unlock/04-01-SUMMARY.md`
</output>
