---
phase: 10-lean-production-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-lean-production-check/scripts/lean-verify.mjs
  - .planning/phases/10-lean-production-check/10-LEAN-CHECK.md
  - .planning/phases/10-lean-production-check/10-01-SUMMARY.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - PROD-03
user_setup: []

must_haves:
  truths:
    - "10-LEAN-CHECK.md proves Absent / Present / Ignores / Status (push-ready ≠ pushed) / Hard stops per D-01"
    - "Orphans absent: no mockups/; no root Hero-background.png; all 55 paths in 05-DELETE-LIST.txt absent"
    - "Keep set present: all 82 inventory class=keep paths exist; web/public file count matches (ignore .DS_Store); mockups.css + mockups-main.js + public hero PNG + deployable web/ present"
    - "Ignores: node_modules / .next / .env* check-ignored and untracked under web/"
    - "lean-verify.mjs exits 0 (D-01 asserts + npm run build) per D-02"
    - "Hard stops (D-03): docs/ and wireframes/ still present; no git push; no mockups/ restore; no further web/public prune"
    - "Branch ahead of origin is recorded as OK — push-ready ≠ pushed"
  artifacts:
    - path: ".planning/phases/10-lean-production-check/scripts/lean-verify.mjs"
      provides: "Nyquist PROD-03 gate (absence/presence/ignores/status + build)"
      min_lines: 100
      contains: "skip-build"
    - path: ".planning/phases/10-lean-production-check/10-LEAN-CHECK.md"
      provides: "Human-readable D-01 lean / push-ready proof"
      min_lines: 40
      contains: "push-ready"
    - path: ".planning/phases/10-lean-production-check/10-01-SUMMARY.md"
      provides: "PROD-03 evidence + D-03 attestations"
      min_lines: 25
      contains: "PROD-03"
  key_links:
    - from: ".planning/phases/10-lean-production-check/scripts/lean-verify.mjs"
      to: ".planning/phases/02-asset-inventory/02-INVENTORY.json"
      via: "assert every class=keep path exists; keep count === 82"
      pattern: "class.*keep|INVENTORY"
    - from: ".planning/phases/10-lean-production-check/scripts/lean-verify.mjs"
      to: ".planning/phases/05-prune-public-assets/05-DELETE-LIST.txt"
      via: "assert every delete-list path absent"
      pattern: "DELETE-LIST|delete.list"
    - from: ".planning/phases/10-lean-production-check/scripts/lean-verify.mjs"
      to: "web/"
      via: "spawn npm run build unless --skip-build"
      pattern: "npm.*build|skip-build"
    - from: ".planning/phases/10-lean-production-check/10-LEAN-CHECK.md"
      to: "scripts/lean-verify.mjs"
      via: "checklist rows mirror script asserts"
      pattern: "lean-verify|Absent|Present|Ignores"
---

<objective>
Satisfy **PROD-03**: confirm the production repo is lean and **push-ready** for deploying `web/` — milestone orphans gone, keep set intact, ignores exclude install/build junk, status coherent — without pushing and without deleting `docs/` or `wireframes/`.

Purpose: Package evidence (checklist + Nyquist script + build) that Phases 3/5/9 cleanup already holds; do not redesign or re-prune.
Output: `scripts/lean-verify.mjs`, `10-LEAN-CHECK.md`, build-green proof, `10-01-SUMMARY.md` (+ optional PROJECT.md Current State wrap).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-lean-production-check/10-CONTEXT.md
@.planning/phases/10-lean-production-check/10-RESEARCH.md
@.planning/phases/10-lean-production-check/10-VALIDATION.md
@.planning/phases/02-asset-inventory/02-INVENTORY.json
@.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt
@.planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs
@.planning/codebase/TESTING.md

<interfaces>
<!-- Locked paths + gate contract — use directly; do not invent alternates -->

REPO_ROOT from script (phase-local, same depth as Phases 6–9):
```text
.planning/phases/10-lean-production-check/scripts/lean-verify.mjs
REPO_ROOT = path.resolve(__dirname, "../../../..")
```

SoT inputs (read-only):
```text
.planning/phases/02-asset-inventory/02-INVENTORY.json     → class=keep (82)
.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt → 55 paths must stay absent
```

ABSENT (must fail if present):
```text
mockups/                         (repo root)
Hero-background.png              (repo root only)
every line in 05-DELETE-LIST.txt (under web/public as listed)
```

PRESENT (must fail if missing):
```text
web/src/
web/package.json
web/next.config.ts               (or next.config.*)
web/public/mockups.css
web/src/lib/mockups-main.js
web/public/assets/hero/Hero-background.png
every inventory class=keep path
web/public file count === keep count (exclude .DS_Store)
```

IGNORES (must prove):
```text
(cd web && git check-ignore -v node_modules .next .env .env.local)
git ls-files 'web/node_modules' 'web/.next' 'web/.env*'   → empty
```

ALLOW (must NOT fail):
```text
docs/          may exist — D-03
wireframes/    may exist — D-03
main ahead of origin/main — D-01.5 OK
```

HARD STOPS (D-03 — never do):
```text
Do not delete docs/ or wireframes/
Do not git push
Do not restore mockups/
Do not prune further under web/public beyond Phase 5 list
Do not add Vitest/Playwright
```

Usage:
```bash
node .planning/phases/10-lean-production-check/scripts/lean-verify.mjs --skip-build   # quick
node .planning/phases/10-lean-production-check/scripts/lean-verify.mjs                 # full = asserts + build
```
</interfaces>
</context>

<tasks>

<task type="auto" id="10-01-01">
  <name>Task 1: Author lean-verify.mjs (Wave 0)</name>
  <files>.planning/phases/10-lean-production-check/scripts/lean-verify.mjs</files>
  <action>
Create phase-local Nyquist gate `scripts/lean-verify.mjs` (D-02; “or equiv” = phase dir per RESEARCH A1 — do **not** create repo-root `scripts/`).

Mirror Phase 9 `remove-verify.mjs` structure: `#!/usr/bin/env node`, ESM imports (`fs`, `path`, `fileURLToPath`, `execFileSync` / `spawnSync`), `fail()` + `process.exit(1)`, `REPO_ROOT = resolve(__dirname, "../../../..")`.

**Security (T-10-01):** `assertNoTraversal` — reject any controlled path/`..` segment; resolve only under `REPO_ROOT`; use `execFileSync` argv arrays (no shell string concat for path-bearing args).

**Asserts (D-01):**
1. ABSENT: `mockups/` not on disk + `git ls-files mockups` empty; root `Hero-background.png` absent; every non-empty line of `05-DELETE-LIST.txt` absent on disk.
2. PRESENT: `web/src`, `web/package.json`, Next config (`web/next.config.ts` or `next.config.*`); KEEP ports `web/public/mockups.css`, `web/src/lib/mockups-main.js`, `web/public/assets/hero/Hero-background.png`; parse `02-INVENTORY.json` and assert every `class === "keep"` path exists; keep count === inventory keep length (expect **82**); `web/public` recursive file count equals keep count ignoring `.DS_Store`.
3. IGNORES: `git check-ignore` under `web/` for `node_modules`, `.next`, `.env`, `.env.local`; `git ls-files` empty for `web/node_modules`, `web/.next`, `web/.env*`.
4. ALLOW: if `docs/` or `wireframes/` exist, log ALLOW — never `assertAbsent` them (D-03).
5. STATUS: log `git rev-list --left-right --count origin/main...HEAD` (or status -sb); **do not fail** solely because ahead &gt; 0 (D-01.5).
6. BUILD: unless argv includes `--skip-build`, run `npm run build` with `cwd: web/` and require exit 0 (D-02).

Print a short PASS summary (counts: keep, delete-list absent, ahead/behind, build skipped|ok). No Vitest. Do not modify app source or delete anything in this task.
  </action>
  <verify>
    <automated>node --check .planning/phases/10-lean-production-check/scripts/lean-verify.mjs &amp;&amp; node .planning/phases/10-lean-production-check/scripts/lean-verify.mjs --skip-build</automated>
  </verify>
  <done>Script syntax-clean; --skip-build exits 0 against current tree; path traversal rejected; D-01 FS/git asserts encoded; build gated behind default (no --skip-build).</done>
</task>

<task type="auto" id="10-01-02">
  <name>Task 2: Write 10-LEAN-CHECK.md (+ optional hygiene)</name>
  <files>.planning/phases/10-lean-production-check/10-LEAN-CHECK.md</files>
  <action>
Write `10-LEAN-CHECK.md` as the primary D-01 deliverable. Structure sections exactly:

1. **Absent** — commands/results for `mockups/`, root `Hero-background.png`, delete-list (55 absent). Record PASS.
2. **Present** — deployable `web/` (`src/`, package.json, next.config), keep set **82**/82, `mockups.css` + `mockups-main.js` + public hero PNG. Record PASS.
3. **Ignores** — `check-ignore` + empty `ls-files` for node_modules / .next / .env*. Record PASS.
4. **Status** — branch vs origin; explicitly state **push-ready ≠ pushed**; ahead-of-origin is OK (D-01.5).
5. **Hard stops** — attest `docs/` and `wireframes/` remain; no push; no mockups restore; no further public prune (D-03).

Mirror script asserts so a verifier can re-run either layer. Include observed counts (keep=82, delete absent=55) and pointer to `lean-verify.mjs`.

**Discretion:** optionally `rm` untracked root/docs `.DS_Store` only if present and untracked — do **not** block on historical tracked `.planning/**/.DS_Store`. Do **not** delete `docs/` or `wireframes/`. Do **not** change files under `web/public` or restore `mockups/`.

Re-run quick gate after writing: `node .../lean-verify.mjs --skip-build`.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-lean-production-check/10-LEAN-CHECK.md &amp;&amp; test -d docs &amp;&amp; test -d wireframes &amp;&amp; node .planning/phases/10-lean-production-check/scripts/lean-verify.mjs --skip-build</automated>
  </verify>
  <done>Checklist exists with Absent/Present/Ignores/Status/Hard stops; docs+wireframes preserved; skip-build still green.</done>
</task>

<task type="auto" id="10-01-03">
  <name>Task 3: Full lean-verify + build + SUMMARY</name>
  <files>.planning/phases/10-lean-production-check/10-01-SUMMARY.md, .planning/PROJECT.md</files>
  <action>
Run the **full** gate (D-02): `node .planning/phases/10-lean-production-check/scripts/lean-verify.mjs` (no `--skip-build`) so D-01 asserts **and** `cd web && npm run build` both exit 0. If script already shells build, do not skip it.

Write `10-01-SUMMARY.md` with: PROD-03 outcome; lean-verify + build exit 0; key counts (keep 82, delete-list 55 absent, public file count); Status (ahead OK / push-ready ≠ pushed); **D-03 attestations** — no `git push` this phase, `docs/`+`wireframes/` still present, no `mockups/` restore, no additional `web/public` deletions in phase commits.

**Discretion:** optionally add one short Current State line in `.planning/PROJECT.md` that PROD-03 lean check is done and push awaits user request — do **not** claim remote-deployed or mark the phase complete in ROADMAP.

Never run `git push`. Never delete `docs/` or `wireframes/`. Never prune `web/public`.
  </action>
  <verify>
    <automated>node .planning/phases/10-lean-production-check/scripts/lean-verify.mjs &amp;&amp; test -f .planning/phases/10-lean-production-check/10-LEAN-CHECK.md &amp;&amp; test -f .planning/phases/10-lean-production-check/10-01-SUMMARY.md &amp;&amp; test -d docs &amp;&amp; test -d wireframes</automated>
  </verify>
  <done>Full lean-verify (with build) exit 0; SUMMARY documents PROD-03 + D-03 no-push / preserve attestations; checklist still present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → filesystem/git | Script reads inventory/delete-list and probes repo paths; must not escape REPO_ROOT |
| verify script → npm build | Spawns local Next build; no network deploy / no push |
| checklist / SUMMARY → humans | Attestations only; must not instruct push or destructive deletes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `scripts/lean-verify.mjs` | mitigate | Reject `..` in controlled paths; resolve under REPO_ROOT; execFile argv only |
| T-10-02 | I | ignore asserts / `.env*` | mitigate | `git check-ignore` + empty `git ls-files` for `web/node_modules`, `web/.next`, `web/.env*` |
| T-10-03 | T/D | accidental delete of docs/wireframes/ports | mitigate | D-03 hard stops in tasks; script ALLOW docs/wireframes; KEEP asserts for mockups.css / mockups-main.js; no public prune tasks |
| T-10-04 | E | unauthorized `git push` | mitigate | D-03; no push in any task; SUMMARY attestation that push was not performed |
</threat_model>

<verification>
Full suite before phase verify-work:
```bash
node .planning/phases/10-lean-production-check/scripts/lean-verify.mjs
test -f .planning/phases/10-lean-production-check/10-LEAN-CHECK.md
test -f .planning/phases/10-lean-production-check/10-01-SUMMARY.md
test -d docs && test -d wireframes
```
</verification>

<success_criteria>
- PROD-03 satisfied via checklist + lean-verify (asserts + build) exit 0
- D-01…D-03 honored (proof artifact, automated gate, hard stops)
- Push-ready documented without pushing; docs/wireframes retained
</success_criteria>

<output>
After completion, create `.planning/phases/10-lean-production-check/10-01-SUMMARY.md`
</output>
