---
phase: 03-safe-orphan-deletes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/styles/mockups.css
  - Hero-background.png
autonomous: true
requirements:
  - ASSET-03
  - ASSET-04
user_setup: []

must_haves:
  truths:
    - "web/src/styles/mockups.css no longer exists on disk"
    - "No web/src module imports styles/mockups or src/styles/mockups.css"
    - "web/public/mockups.css still exists and layout.tsx still links href=\"/mockups.css\""
    - "Repo-root Hero-background.png no longer exists on disk"
    - "web/public/assets/hero/Hero-background.png still exists and CinematicHero still references /assets/hero/Hero-background.png"
    - "web/src/styles/cinematic-hero.css still exists (styles/ dir kept when non-empty)"
    - "No Phase 2 inventory public delete-candidate paths were deleted"
    - "npm run build succeeds in web/"
  artifacts:
    - path: "web/public/mockups.css"
      provides: "Served Floating Sanctuary stylesheet (KEEP)"
      min_lines: 1
    - path: "web/public/assets/hero/Hero-background.png"
      provides: "Live cinematic hero PNG (KEEP)"
    - path: "web/src/app/layout.tsx"
      provides: "App layout that links /mockups.css"
      contains: "href=\"/mockups.css\""
    - path: "web/src/components/CinematicHero.tsx"
      provides: "Hero that references public hero PNG"
      contains: "/assets/hero/Hero-background.png"
    - path: "web/src/styles/cinematic-hero.css"
      provides: "Live cinematic hero CSS (must survive mockups.css delete)"
      min_lines: 1
  key_links:
    - from: "web/src/app/layout.tsx"
      to: "web/public/mockups.css"
      via: "link rel=stylesheet href=/mockups.css"
      pattern: "href=\"/mockups.css\""
    - from: "web/src/components/CinematicHero.tsx"
      to: "web/public/assets/hero/Hero-background.png"
      via: "next/image src=/assets/hero/Hero-background.png"
      pattern: "/assets/hero/Hero-background\\.png"
    - from: "web/src/components/CinematicHero.tsx"
      to: "web/src/styles/cinematic-hero.css"
      via: "import @/styles/cinematic-hero.css"
      pattern: "@/styles/cinematic-hero\\.css"
---

<objective>
Remove two confirmed non-runtime orphans — unused `web/src/styles/mockups.css` (ASSET-03) and repo-root `Hero-background.png` (ASSET-04) — in one commit, without touching served public assets or Phase 2 inventory delete candidates.

Purpose: Cut ~2.4MB of proven-dead weight before Phase 4–5 public prune work, while fail-closing on missing imports or layout/CSS breakage via `npm run build`.
Output: Both orphans gone from disk; keep paths intact; one coherent git commit; `03-01-SUMMARY.md` with verification evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-safe-orphan-deletes/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md

@web/src/app/layout.tsx
@web/src/components/CinematicHero.tsx
@web/src/styles/mockups.css
@web/public/mockups.css
@web/public/assets/hero/Hero-background.png

## Locked decisions (NON-NEGOTIABLE)

- **D-01:** One coherent commit removes both orphans (and empty `web/src/styles/` only if applicable).
- **D-02:** After deleting `web/src/styles/mockups.css`, remove `web/src/styles/` **only if it becomes empty**.
- **D-03:** Verify with file/git checks **and** `npm run build` in `web/`.

## Claude's Discretion (apply as written)

- Before delete: reconfirm zero `web/src` import/reference to `styles/mockups` or `src/styles/mockups.css`.
- After delete: confirm `layout.tsx` still links `/mockups.css`; public hero PNG still exists and is referenced by `CinematicHero.tsx`.
- Do **not** stage or delete any path from `02-INVENTORY.json` delete class (Phase 5).

## Exact allowlist — DELETE only these

1. `web/src/styles/mockups.css`
2. `Hero-background.png` (repo root)

## Exact denylist — NEVER delete in this plan

- `web/public/mockups.css`
- `web/public/assets/hero/Hero-background.png`
- `web/src/styles/cinematic-hero.css` (still imported by `CinematicHero.tsx`)
- Any path under `web/public/` that appears in Phase 2 inventory delete class
- `mockups/` tree

## Known on-disk note (planner verification 2026-09-05)

- `web/src/styles/` currently contains **both** `mockups.css` (delete) and `cinematic-hero.css` (keep). After mockups delete, `styles/` will **not** be empty — do **not** remove the directory (D-02 conditional).
- Repo-root `Hero-background.png` is **untracked** (`??`). Delete from disk anyway; the single commit will primarily record the tracked CSS removal. SUMMARY must note root PNG was untracked and removed from working tree.

<interfaces>
<!-- Contracts executors must preserve — extracted from codebase -->

From web/src/app/layout.tsx:
```tsx
<link rel="stylesheet" href="/mockups.css" />
```

From web/src/components/CinematicHero.tsx:
```tsx
import "@/styles/cinematic-hero.css";
// ...
src="/assets/hero/Hero-background.png"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-delete import scan and keep-path proof</name>
  <files>.planning/phases/03-safe-orphan-deletes/.predelete-scan.txt</files>
  <read_first>.planning/phases/03-safe-orphan-deletes/03-CONTEXT.md, web/src/app/layout.tsx, web/src/components/CinematicHero.tsx, .planning/codebase/CONCERNS.md</read_first>
  <action>
Per Claude's discretion and ASSET-03: prove the CSS orphan is unused and keep targets are present **before** any delete.

1. From repo root, run a scan of `web/src` for references to the dead CSS path (not the public `/mockups.css` link):
   - `rg -n 'styles/mockups|src/styles/mockups\.css|@/styles/mockups' web/src || true`
   - Expected: **zero** matches that import or reference `styles/mockups` / `src/styles/mockups.css` / `@/styles/mockups`. Comment-only mentions of the *served* `mockups.css` filename (e.g. in `globals.css`) are OK and are **not** grounds to abort — abort only on import/path references to the `src/styles/mockups` copy.
2. Confirm delete targets exist:
   - `test -f web/src/styles/mockups.css`
   - `test -f Hero-background.png`
3. Confirm keep targets exist and are wired:
   - `test -f web/public/mockups.css`
   - `test -f web/public/assets/hero/Hero-background.png`
   - `test -f web/src/styles/cinematic-hero.css`
   - `rg -n 'href="/mockups.css"' web/src/app/layout.tsx` must match
   - `rg -n '/assets/hero/Hero-background\.png' web/src/components/CinematicHero.tsx` must match
   - `rg -n '@/styles/cinematic-hero\.css' web/src/components/CinematicHero.tsx` must match
4. Write results to `.planning/phases/03-safe-orphan-deletes/.predelete-scan.txt` (commands run + exit outcomes). If any keep check fails or a real `src/styles/mockups` import is found → **STOP** — do not delete.

Do not delete anything in this task.
  </action>
  <verify>
    <automated>cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs" && test -f .planning/phases/03-safe-orphan-deletes/.predelete-scan.txt && test -f web/src/styles/mockups.css && test -f Hero-background.png && test -f web/public/mockups.css && test -f web/public/assets/hero/Hero-background.png && ! rg -q 'styles/mockups|src/styles/mockups\.css|@/styles/mockups' web/src --glob '!**/styles/mockups.css'</automated>
  </verify>
  <acceptance_criteria>
    - `.predelete-scan.txt` exists under `.planning/phases/03-safe-orphan-deletes/`
    - `rg -n 'styles/mockups|src/styles/mockups\.css|@/styles/mockups' web/src --glob '!**/styles/mockups.css'` returns exit 1 (no matches)
    - `test -f web/public/mockups.css` and `test -f web/public/assets/hero/Hero-background.png` both succeed
    - `web/src/app/layout.tsx` still contains `href="/mockups.css"`
  </acceptance_criteria>
  <done>Scan artifact recorded; zero src imports of dead CSS copy; both orphans and all keep paths confirmed present; no deletes yet.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphans, apply D-02 empty-dir rule, single commit</name>
  <files>web/src/styles/mockups.css, Hero-background.png</files>
  <read_first>.planning/phases/03-safe-orphan-deletes/.predelete-scan.txt, .planning/phases/03-safe-orphan-deletes/03-CONTEXT.md</read_first>
  <action>
Execute deletes **only** for the allowlist (ASSET-03, ASSET-04). Per D-01 / D-02.

1. Delete files:
   - `git rm -f web/src/styles/mockups.css` (tracked)
   - `rm -f Hero-background.png` (untracked — plain rm)
2. Empty-dir rule (D-02): If `web/src/styles/` has **no remaining files**, remove the empty directory. If `cinematic-hero.css` (or any other file) remains, **keep** `web/src/styles/`. Expected outcome given current tree: directory remains with `cinematic-hero.css` only.
3. Hard stop checks after delete:
   - `test ! -f web/src/styles/mockups.css`
   - `test ! -f Hero-background.png`
   - `test -f web/public/mockups.css`
   - `test -f web/public/assets/hero/Hero-background.png`
   - `test -f web/src/styles/cinematic-hero.css`
   - Confirm `git status --short` does **not** show deletions under `web/public/` (no Phase 2 inventory candidates staged/deleted)
4. Single coherent commit (D-01). Stage only the orphan removals (CSS deletion via git; root PNG already gone from disk). Message via HEREDOC, e.g.:

```
chore(03-01): remove unused src mockups.css and root hero PNG orphan

ASSET-03/ASSET-04: drop dead web/src/styles/mockups.css and untracked
repo-root Hero-background.png; keep served public CSS and live hero PNG.
```

Do not amend. Do not push. Do not touch `02-INVENTORY.json` delete paths.
  </action>
  <verify>
    <automated>cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs" && test ! -f web/src/styles/mockups.css && test ! -f Hero-background.png && test -f web/public/mockups.css && test -f web/public/assets/hero/Hero-background.png && test -f web/src/styles/cinematic-hero.css && ! git ls-files --error-unmatch web/src/styles/mockups.css</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/styles/mockups.css` is absent from disk and from `git ls-files`
    - `Hero-background.png` at repo root is absent from disk
    - `web/public/mockups.css` and `web/public/assets/hero/Hero-background.png` still exist
    - `web/src/styles/cinematic-hero.css` still exists
    - `git log -1 --oneline` message references both orphans / ASSET-03/ASSET-04 (or equivalent chore(03-01) message)
    - `git show --name-status --pretty=format: HEAD` does not list any `web/public/` deletion
  </acceptance_criteria>
  <done>Both orphans deleted; keep paths intact; styles/ retained if non-empty; one commit created covering the cleanup.</done>
</task>

<task type="auto">
  <name>Task 3: Post-delete file/git checks and web build (D-03)</name>
  <files>.planning/phases/03-safe-orphan-deletes/03-01-SUMMARY.md</files>
  <read_first>web/src/app/layout.tsx, web/src/components/CinematicHero.tsx, web/package.json</read_first>
  <action>
Per D-03: fail-closed proof after deletes.

1. File/git checks (all must pass):
   - `test ! -e web/src/styles/mockups.css`
   - `test ! -e Hero-background.png`
   - `test -f web/public/mockups.css`
   - `test -f web/public/assets/hero/Hero-background.png`
   - `rg -n 'href="/mockups.css"' web/src/app/layout.tsx`
   - `rg -n '/assets/hero/Hero-background\.png' web/src/components/CinematicHero.tsx`
   - `rg -n '@/styles/cinematic-hero\.css' web/src/components/CinematicHero.tsx`
   - `! rg -q 'styles/mockups|src/styles/mockups\.css|@/styles/mockups' web/src`
2. Build: `cd web && npm run build` — must exit 0.
3. Write `.planning/phases/03-safe-orphan-deletes/03-01-SUMMARY.md` with: commit SHA, list of deleted paths, confirmation keep paths remain, note that root PNG was untracked, build command + result, explicit statement that no `web/public` inventory candidates were deleted.

Optional: delete `.predelete-scan.txt` after SUMMARY is written, or leave it untracked — do not commit inventory-delete paths.
  </action>
  <verify>
    <automated>cd "/Users/usama/Desktop/Dominion Markets/Vanilla to nextjs/web" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` in `web/` exits 0
    - `03-01-SUMMARY.md` exists and records commit SHA + deleted paths + keep-path confirmation
    - `web/src/app/layout.tsx` still contains `href="/mockups.css"`
    - `CinematicHero.tsx` still contains `/assets/hero/Hero-background.png`
    - Root `Hero-background.png` and `web/src/styles/mockups.css` remain absent
  </acceptance_criteria>
  <done>Build green; SUMMARY written with D-03 evidence; live CSS/hero wiring unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → working tree | Agent may delete files; only allowlisted orphans may be removed |
| App → public assets | Runtime serves `web/public/*`; deleting served CSS/hero would break production UX |
| Git index → history | Commit must not record accidental `web/public` mass deletes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `web/public/mockups.css`, `web/public/assets/hero/Hero-background.png` | mitigate | Task 1 keep-path existence checks; Task 2 hard-stop `test -f` on keep paths; denylist forbids public deletes; acceptance requires keep files present |
| T-03-02 | Tampering | Phase 2 `02-INVENTORY.json` delete class under `web/public/` | mitigate | Explicit denylist + post-delete `git show` must not list `web/public/` deletions; only allowlist paths may be removed |
| T-03-03 | Denial of service | App build / layout CSS link | mitigate | Task 3 runs `npm run build` in `web/`; verify `layout.tsx` still has `href="/mockups.css"` and cinematic-hero CSS import remains |
| T-03-04 | Information disclosure | N/A — binary/CSS orphans only | accept | No secrets in target files; low sensitivity marketing assets |
| T-03-05 | Elevation of privilege | N/A — local filesystem cleanup | accept | No auth or privilege boundary crossed |
| T-03-06 | Spoofing | N/A — no identity surface | accept | Delete operations are local path ops only |
| T-03-07 | Repudiation | Git commit of CSS delete | mitigate | Single coherent commit (D-01) with ASSET-03/ASSET-04 message; SUMMARY records SHA and untracked root PNG removal |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `web/src/styles/mockups.css` absent; `Hero-background.png` (root) absent
- [ ] `web/public/mockups.css` and `web/public/assets/hero/Hero-background.png` present
- [ ] `layout.tsx` links `/mockups.css`; `CinematicHero.tsx` references public hero PNG
- [ ] No `web/public/` deletions in the phase commit
- [ ] `cd web && npm run build` exits 0
</verification>

<success_criteria>
- ASSET-03 satisfied: dead `web/src/styles/mockups.css` removed after import scan
- ASSET-04 satisfied: root `Hero-background.png` removed; public hero PNG kept
- D-01/D-02/D-03 honored (one commit; empty styles/ only if empty; file+git+build proof)
- No Phase 2 public inventory candidates deleted
- `03-01-SUMMARY.md` written with evidence
</success_criteria>

<source_audit>
SOURCE    | ID      | Item                                      | Plan | Status  | Notes
--------- | ------- | ----------------------------------------- | ---- | ------- | -----
GOAL      | —       | Confirmed non-runtime duplicates gone     | 01   | COVERED | CSS + root PNG orphans
REQ       | ASSET-03| Remove dead web/src/styles/mockups.css    | 01   | COVERED | Tasks 1–3
REQ       | ASSET-04| Remove root Hero-background.png           | 01   | COVERED | Tasks 2–3
RESEARCH  | —       | (skipped)                                 | —    | N/A     | Orchestrator: research skipped
CONTEXT   | D-01    | One commit for both deletes               | 01   | COVERED | Task 2
CONTEXT   | D-02    | Remove empty web/src/styles/ if empty     | 01   | COVERED | Task 2 conditional (cinematic-hero remains)
CONTEXT   | D-03    | File/git checks + npm run build           | 01   | COVERED | Task 3
</source_audit>

<output>
After completion, create `.planning/phases/03-safe-orphan-deletes/03-01-SUMMARY.md`
</output>
