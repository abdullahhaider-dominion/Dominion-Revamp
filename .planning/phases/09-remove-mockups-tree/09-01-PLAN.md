---
phase: 09-remove-mockups-tree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs
  - mockups/
  - readme.md
  - web/README.md
  - .planning/PROJECT.md
  - .cursor/skills/dominion-section-redesign/SKILL.md
  - CLAUDE.md
  - .planning/phases/09-remove-mockups-tree/09-01-SUMMARY.md
autonomous: true
requirements:
  - MOCK-03
user_setup: []

must_haves:
  truths:
    - "D-01 preflight passed before any delete: archive/mockups resolves; sibling HEAD matches tip; 07-ARCHIVE.md exists; five operational targets cite 07-ARCHIVE.md"
    - "mockups/ is absent from the working tree (test ! -e mockups) and git ls-files mockups is empty after git rm -r mockups"
    - "web/public/mockups.css and web/src/lib/mockups-main.js still exist (KEEP — production ports, not the mockups/ tree)"
    - "Operational docs no longer say in-repo mockups/ may exist / temporary until Phase 9; still point at ARCHIVE + web/; no in-repo SoT reintroduced"
    - "archive/mockups tip and sibling HEAD unchanged; no git push; no web/src edits (unless abort-on-dependency)"
    - "remove-verify.mjs exits 0 and cd web && npm run build exits 0"
  artifacts:
    - path: ".planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs"
      provides: "Post-delete Nyquist gate for MOCK-03"
      min_lines: 80
      contains: "mockups.css"
    - path: "web/public/mockups.css"
      provides: "Production Floating Sanctuary CSS (KEEP)"
      contains: ""
    - path: "web/src/lib/mockups-main.js"
      provides: "Production interactions port (KEEP)"
      contains: ""
    - path: "readme.md"
      provides: "Root SoT pointer without until-Phase-9 presence language"
      contains: "07-ARCHIVE.md"
    - path: "web/README.md"
      provides: "web/ Notes SoT without until-Phase-9 presence language"
      contains: "07-ARCHIVE.md"
    - path: ".planning/PROJECT.md"
      provides: "Project context tense cleaned; ARCHIVE + web/"
      contains: "07-ARCHIVE.md"
    - path: ".cursor/skills/dominion-section-redesign/SKILL.md"
      provides: "Skill tense cleaned; ARCHIVE + web/"
      contains: "07-ARCHIVE.md"
    - path: "CLAUDE.md"
      provides: "Operational agent tense cleaned"
      contains: "07-ARCHIVE.md"
    - path: ".planning/phases/09-remove-mockups-tree/09-01-SUMMARY.md"
      provides: "MOCK-03 evidence + D-05 attestations"
      min_lines: 25
      contains: "MOCK-03"
  key_links:
    - from: ".planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs"
      to: "mockups/"
      via: "asserts path absent + git ls-files empty"
      pattern: "existsSync.*mockups|ls-files.*mockups"
    - from: ".planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs"
      to: "web/public/mockups.css"
      via: "KEEP assert after delete"
      pattern: "mockups\\.css"
    - from: "readme.md"
      to: ".planning/phases/07-archive-mockups/07-ARCHIVE.md"
      via: "still cites ARCHIVE; no temporary until Phase 9"
      pattern: "07-ARCHIVE\\.md"
    - from: "git rm -r mockups"
      to: "working tree"
      via: "D-02 coherent delete commit"
      pattern: ""
---

<objective>
Satisfy **MOCK-03**: remove in-repo `mockups/` from the production working tree only after Phase 7 archive + Phase 8 doc retarget. Prove archive still reachable, KEEP production ports (`web/public/mockups.css`, `web/src/lib/mockups-main.js`), clean “until Phase 9” tense, and confirm `web/` builds with no runtime dependency on `../mockups`.

Purpose: Slim the production repo; design SoT remains `archive/mockups` + sibling + `07-ARCHIVE.md`.
Output: `scripts/remove-verify.mjs`, deleted `mockups/` tree, tense-cleaned operational docs, `09-01-SUMMARY.md` with MOCK-03 evidence.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-remove-mockups-tree/09-CONTEXT.md
@.planning/phases/09-remove-mockups-tree/09-RESEARCH.md
@.planning/phases/09-remove-mockups-tree/09-VALIDATION.md
@.planning/phases/07-archive-mockups/07-ARCHIVE.md
@.planning/phases/07-archive-mockups/scripts/archive-verify.mjs

<interfaces>
<!-- Locked coordinates + gates — use directly; do not invent alternate paths -->

Archive tip (re-check at execute — must match sibling HEAD and 07-ARCHIVE.md):
```text
3e9401bd85c6848cbbf073d526e5d48ad2450834
```

Sibling path (D-01 / D-05 — fixed constant; never modify contents):
```text
/Users/usama/Desktop/Dominion Markets/Dominion-Markets-mockups-archive
```

DELETE vs KEEP (name collision — critical):
```text
DELETE:  mockups/**                         (repo-root tree only)
KEEP:    web/public/mockups.css             → layout <link href="/mockups.css">
KEEP:    web/src/lib/mockups-main.js        → DominionHome dynamic import
GONE OK: web/src/styles/mockups.css         (already deleted ASSET-03)
```

Operational doc targets (D-03 only — do not rewrite docs/superpowers/** or Phase 7/8 historical narratives):
```text
readme.md
web/README.md
.planning/PROJECT.md
.cursor/skills/dominion-section-redesign/SKILL.md
CLAUDE.md
```

Canonical post–D-03 one-liner (prefer):
```text
Hi-fi design archive: see `.planning/phases/07-archive-mockups/07-ARCHIVE.md`
(`archive/mockups` / sibling repo). Production site: `web/`.
```

Deny-list phrases (must be absent from the five operational targets after Task 2):
```text
until Phase 9
temporary until Phase 9
may still exist temporarily
may remain until Phase 9
may exist until Phase 9
still present on main until Phase 9
```

Hard stops (D-05):
- Never delete/modify `archive/mockups` branch or the sibling archive repo
- Never `git push`
- Never edit `web/src` except if a true runtime `../mockups` dependency is found → **abort and report** (do not invent ports)
- Never use Phase 7 `archive-verify.mjs` as post-delete success gate (it expects 139 files on main)
- Do **not** flip `.planning/REQUIREMENTS.md` MOCK-03 checkbox or mark Phase 9 complete in ROADMAP — orchestrator/verify-work owns phase close; SUMMARY may note MOCK-03 evidence only

Pattern reference — mirror Phase 7 script security:
```javascript
// Fixed SIBLING_PATH constant; REPO_ROOT = resolve(__dirname, "../../../..")
// assertNoTraversal; execFileSync("git", args) — no shell interpolation
// Reject argv/paths containing '..'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author remove-verify.mjs + D-01 preflight</name>
  <files>.planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs</files>
  <action>
Create `.planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs` (Claude's discretion name per CONTEXT — use this path). Wave 0 Nyquist gate for **post-delete** MOCK-03. Mirror Phase 7 `archive-verify.mjs` structure (Node ESM, `execFileSync`, fixed sibling constant, reject `..` — T-09-01).

**Script MUST assert (exit 0 only when all pass):**
1. `refs/heads/archive/mockups` resolves; tip SHA matches the tip recorded in `07-ARCHIVE.md` (parse SHA from ARCHIVE; do not hardcode only — re-check live)
2. Sibling path exists, is a git repo, `HEAD ===` archive tip
3. Repo-root `mockups/` does **not** exist (`!fs.existsSync(join(REPO_ROOT, "mockups"))`)
4. `git ls-files mockups` is empty
5. `web/public/mockups.css` and `web/src/lib/mockups-main.js` **exist** (KEEP — fail loud if missing)
6. Current branch is **not** `archive/mockups`
7. Each operational target contains `07-ARCHIVE.md`
8. Deny-list: none of the “until Phase 9 / may still exist temporarily / …” phrases appear in the five operational targets
9. Path-traversal: reject `..` in script-controlled paths (same pattern as T-07-01)

**Do not** assert 139 files on main. **Do not** require Phase 7 `archive-verify.mjs` post-delete.

**Then run D-01 preflight BEFORE any delete** (per D-01 — hard gate). From repo root:

```bash
TIP=$(git rev-parse archive/mockups)
SIB="/Users/usama/Desktop/Dominion Markets/Dominion-Markets-mockups-archive"
test -d "$SIB"
test "$(cd "$SIB" && git rev-parse HEAD)" = "$TIP"
test -f .planning/phases/07-archive-mockups/07-ARCHIVE.md
for f in readme.md web/README.md .planning/PROJECT.md \
  .cursor/skills/dominion-section-redesign/SKILL.md CLAUDE.md; do
  rg -q '07-ARCHIVE\.md' "$f" || exit 1
done
```

Optional: `node .planning/phases/07-archive-mockups/scripts/archive-verify.mjs` (preflight only — expects 139 on main).

**Abort if runtime dependency:** `rg -n '\.\./mockups' web/src` — comment provenance in headers is OK; any real import of `../mockups` → stop and report (D-05). Do not delete.

Also: `node --check` on the new script. Commit the script alone (Wave 0) if desired, or leave uncommitted until Task 2 coherent commit — prefer script committed with Task 2 delete+docs as one coherent commit **or** script-first commit then delete commit; either OK as long as delete is one coherent `git rm` commit per D-02.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs && node --check .planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs && grep -v '^#' .planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs | grep -c 'mockups.css' | awk '{exit ($1<1)?1:0}' && TIP=$(git rev-parse archive/mockups) && SIB="/Users/usama/Desktop/Dominion Markets/Dominion-Markets-mockups-archive" && test -d "$SIB" && test "$(cd "$SIB" && git rev-parse HEAD)" = "$TIP" && test -f .planning/phases/07-archive-mockups/07-ARCHIVE.md && for f in readme.md web/README.md .planning/PROJECT.md .cursor/skills/dominion-section-redesign/SKILL.md CLAUDE.md; do rg -q '07-ARCHIVE\.md' "$f" || exit 1; done && echo PREFLIGHT_OK</automated>
  </verify>
  <done>remove-verify.mjs exists, node --check passes, encodes KEEP/delete/ARCHIVE/deny-list asserts; D-01 preflight OK; no delete yet; no runtime ../mockups import blocking.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: git rm mockups + D-03 tense cleanup</name>
  <files>mockups/, readme.md, web/README.md, .planning/PROJECT.md, .cursor/skills/dominion-section-redesign/SKILL.md, CLAUDE.md</files>
  <action>
**Only after Task 1 preflight passed.** Satisfy D-02 + D-03.

1. Record optional byte savings for SUMMARY (discretion): `du -sh mockups` (~64M expected).
2. Remove tracked tree: `git rm -r mockups` (locked — do **not** use bare `rm -rf` as the primary delete; D-02).
3. Clean leftover untracked `.DS_Store` so the directory is gone: `rm -f mockups/.DS_Store mockups/assets/.DS_Store 2>/dev/null; rm -rf mockups` if empty dirs remain. Confirm `test ! -e mockups`.
4. Confirm KEEP paths still present: `test -f web/public/mockups.css` and `test -f web/src/lib/mockups-main.js`. Never `git rm` under `web/`.
5. **D-03 tense cleanup** on the five operational targets only — remove “temporary until Phase 9 / may still exist / may remain until Phase 9 / still present on main until Phase 9” language. Point only at ARCHIVE + `web/`. Do **not** reintroduce in-repo `mockups/` as SoT. Update PROJECT.md completed-item / Context lines that say “still present until Phase 9” to past tense / archive-only (D-03 includes PROJECT tracker tense).
6. Confirm deny-list `rg` has **no** matches on the five files (PASS = rg exit 1 / no hits). Confirm each still contains `07-ARCHIVE.md`.
7. Confirm `git ls-files mockups` is empty.
8. One coherent commit covering delete + tense docs (+ script if not already committed). Message style: `chore(09-01): remove mockups/ tree after archive (MOCK-03)` or similar. **No** REQUIREMENTS.md checkbox flip. **No** ROADMAP phase-complete mark. **No** `git push`. **No** edits to `archive/mockups`, sibling, or `web/src`.
  </action>
  <verify>
    <automated>test ! -e mockups && test -z "$(git ls-files mockups)" && test -f web/public/mockups.css && test -f web/src/lib/mockups-main.js && ! rg -n -e 'until Phase 9' -e 'temporary until Phase 9' -e 'may still exist temporarily' -e 'may remain until Phase 9' -e 'may exist until Phase 9' -e 'still present on main until Phase 9' readme.md web/README.md .planning/PROJECT.md .cursor/skills/dominion-section-redesign/SKILL.md CLAUDE.md && for f in readme.md web/README.md .planning/PROJECT.md .cursor/skills/dominion-section-redesign/SKILL.md CLAUDE.md; do rg -q '07-ARCHIVE\.md' "$f" || exit 1; done && echo DELETE_TENSE_OK</automated>
  </verify>
  <done>mockups/ gone from FS and git index; KEEP ports present; five docs deny-list clean and still cite 07-ARCHIVE.md; coherent commit created; no push; archive/sibling untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full remove-verify + build + SUMMARY</name>
  <files>.planning/phases/09-remove-mockups-tree/09-01-SUMMARY.md</files>
  <action>
Satisfy D-04 proof + SUMMARY.

1. Record archive tip + sibling HEAD **before** final attest (must equal pre-delete tip; D-05 unchanged).
2. Run full suite:
   - `node .planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs` → exit 0
   - `cd web && npm run build` → exit 0
3. Write `.planning/phases/09-remove-mockups-tree/09-01-SUMMARY.md` including:
   - MOCK-03 evidence (tree gone, ls-files empty, KEEP ports, verify+build exit 0)
   - Archive tip SHA + sibling HEAD (unchanged attestation)
   - Optional `du` byte savings (~64M) if recorded in Task 2
   - Explicit: no `git push`; no archive/sibling mutation; no `web/src` edits
   - Note that REQUIREMENTS MOCK-03 checkbox / ROADMAP phase-complete remain for orchestrator/verify-work — do **not** resolve those checkboxes here
4. Commit SUMMARY only (or with any leftover plan artifacts). Still no push.
  </action>
  <verify>
    <automated>node .planning/phases/09-remove-mockups-tree/scripts/remove-verify.mjs && (cd web && npm run build) && test -f .planning/phases/09-remove-mockups-tree/09-01-SUMMARY.md && grep -v '^#' .planning/phases/09-remove-mockups-tree/09-01-SUMMARY.md | grep -c 'MOCK-03' | awk '{exit ($1<1)?1:0}'</automated>
  </verify>
  <done>remove-verify exit 0; npm run build exit 0; SUMMARY documents MOCK-03 evidence and D-05 attestations; REQUIREMENTS/ROADMAP phase-close left for verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor shell → git index / working tree | `git rm -r mockups` mutates production tree; must not touch archive branch or remotes |
| Verify script → fixed roots | Reads git + sibling + KEEP paths; must not accept traversal / arbitrary delete roots |
| Name collision `*mockups*` under `web/` | Production ports must survive; false-positive delete is high impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | `scripts/remove-verify.mjs` | mitigate | Fixed sibling constant; reject `..` in path joins; `execFile` argv only (same as T-07-01) |
| T-09-02 | T / D | Accidental delete of `web/public/mockups.css` or `mockups-main.js` | mitigate | KEEP asserts in remove-verify; never `git rm` under `web/`; post-delete `test -f` gates |
| T-09-03 | T / D | Mutating `archive/mockups` or sibling | mitigate | D-05 hard stop; verify tip unchanged; never checkout archive to delete; never `rm -rf` sibling |
| T-09-04 | E / I | Accidental `git push` of slimmed tree | mitigate | D-05: no push in tasks; SUMMARY attestation; no remote-tracking updates |
| T-09-05 | I | Doc tense still implies tree present | mitigate | Deny-list in remove-verify on five operational targets; D-03 edits before green |
</threat_model>

<verification>
- Preflight (Task 1): D-01 shell gates + `node --check` on remove-verify.mjs
- Mid (Task 2): `test ! -e mockups`; `git ls-files mockups` empty; KEEP files exist; deny-list clean
- Final (Task 3): `node …/remove-verify.mjs` && `cd web && npm run build`
- Confirm archive tip and sibling HEAD unchanged vs pre-delete
- Confirm `git status -sb` shows no push / no new origin/archive tracking from this phase
</verification>

<success_criteria>
- MOCK-03 satisfied: `mockups/` absent from production working tree; `git ls-files mockups` empty
- Archive from Phase 7 still reachable; Phase 8 targets still cite `07-ARCHIVE.md` without “until Phase 9” presence language
- KEEP: `web/public/mockups.css` + `web/src/lib/mockups-main.js` present
- `remove-verify.mjs` exit 0; `cd web && npm run build` exit 0
- No `git push`; archive branch + sibling unmodified; no unexpected `web/src` edits
</success_criteria>

<output>
After completion, create `.planning/phases/09-remove-mockups-tree/09-01-SUMMARY.md`
</output>
