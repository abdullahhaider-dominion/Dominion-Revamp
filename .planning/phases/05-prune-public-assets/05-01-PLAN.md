---
phase: 05-prune-public-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/public/.DS_Store
  - web/public/assets/.DS_Store
  - web/public/assets/awards/laurel.svg
  - web/public/assets/awards/mist-bg.jpg
  - web/public/assets/blogs/mist-bg.png
  - web/public/assets/hero/hero-media.png
  - web/public/assets/hero/hero.mp4
  - web/public/assets/logo/logo-horizontal-color.png
  - web/public/assets/markets-reach/commodities.svg
  - web/public/assets/markets-reach/currencies.svg
  - web/public/assets/markets-reach/discord.svg
  - web/public/assets/markets-reach/mist-bg.jpg
  - web/public/assets/mrkt/mist-bg.jpg
  - web/public/assets/mrkt/product-collage.png
  - web/public/assets/mrkt/product-collage.webp
  - web/public/assets/mt5-sim/mist-bg.jpg
  - web/public/assets/mt5-sim/mt5Logo-transparent.png
  - web/public/assets/mt5-sim/mt5Logo.png
  - web/public/assets/mt5-sim/mt5_section.png
  - web/public/assets/mt5-sim/mt5_trading.png
  - web/public/assets/payments/bitcoin_icon.png
  - web/public/assets/payments/dm-payment-options-darkmode.png
  - web/public/assets/payments/dm-payment-options.png
  - web/public/assets/payments/ethereum_icon.png
  - web/public/assets/payments/funding.png
  - web/public/assets/payments/icons/crypto-pair.png
  - web/public/assets/payments/icons/googlepay.png
  - web/public/assets/payments/icons/mastercard-circles.png
  - web/public/assets/payments/icons/upi.png
  - web/public/assets/payments/mastercard-logo.png
  - web/public/assets/payments/mastercard.svg
  - web/public/assets/payments/mastercard_icon.png
  - web/public/assets/payments/pay-icons-01.png
  - web/public/assets/payments/pay-icons-darkmode-01.png
  - web/public/assets/payments/pay-icons-darkmode.png
  - web/public/assets/payments/pay-icons-new-01-00.png
  - web/public/assets/payments/pay-icons-new-01.png
  - web/public/assets/payments/pay-icons.png
  - web/public/assets/payments/same-day-withdrawls-03.png
  - web/public/assets/payments/visa-logo.png
  - web/public/assets/payments/visa.svg
  - web/public/assets/platforms/ctrader-icon.png
  - web/public/assets/platforms/ctrader-lockup.png
  - web/public/assets/platforms/ctrader-logo2.png
  - web/public/assets/platforms/download-ctrader-logo.png
  - web/public/assets/platforms/mt5-icon.png
  - web/public/assets/platforms/mt5-lockup.png
  - web/public/assets/platforms/platforms-mist-water.jpg
  - web/public/assets/trade-better/icons/bull-wire.svg
  - web/public/assets/trader-community/avatars/avatar-1.webp
  - web/public/assets/trader-community/avatars/avatar-2.webp
  - web/public/assets/trader-community/avatars/avatar-3.webp
  - web/public/assets/trader-community/avatars/avatar-4.webp
  - web/public/assets/trader-community/avatars/avatar-5.webp
  - web/public/assets/trader-community/avatars/avatar-6.webp
  - .planning/phases/05-prune-public-assets/05-01-SUMMARY.md
autonomous: true
requirements:
  - ASSET-02
user_setup: []

must_haves:
  truths:
    - "All 55 still_unreferenced paths from 04-RESCAN.json are absent from disk"
    - "Every 02-INVENTORY.json class=keep path still exists on disk (82 keeps)"
    - "web/public/assets/hero/Hero-background.png, web/public/mockups.css, and inventory logo keep paths remain"
    - "Exactly one git commit removed the 55 allowlisted paths (no keep paths in that commit)"
    - "npm run build succeeds in web/ after the prune (D-01)"
    - "SUMMARY records ASSET-02 complete and ~32.6 MB (34209521 bytes) savings estimate"
  artifacts:
    - path: ".planning/phases/05-prune-public-assets/05-01-SUMMARY.md"
      provides: "Execution proof: delete counts, keep checks, build result, byte savings"
      min_lines: 40
      contains: "ASSET-02"
    - path: "web/public/mockups.css"
      provides: "Served Floating Sanctuary stylesheet (KEEP — never deleted)"
      min_lines: 1
    - path: "web/public/assets/hero/Hero-background.png"
      provides: "Live cinematic hero PNG (KEEP — never deleted)"
    - path: ".planning/phases/04-minimal-react-unlock/04-RESCAN.json"
      provides: "Delete SoT — 55 still_unreferenced candidates"
      contains: "still_unreferenced"
  key_links:
    - from: ".planning/phases/04-minimal-react-unlock/04-RESCAN.json"
      to: "web/public/"
      via: "delete only candidates with status==still_unreferenced"
      pattern: "still_unreferenced"
    - from: ".planning/phases/02-asset-inventory/02-INVENTORY.json"
      to: "delete allowlist"
      via: "set equality with class==delete paths (abort on mismatch)"
      pattern: "\"class\": \"delete\""
    - from: "git commit"
      to: "55 deleted paths only"
      via: "single coherent commit; keep paths untouched"
      pattern: "chore\\(05-01\\)|ASSET-02|prune"
    - from: "web/"
      to: "npm run build"
      via: "D-01 post-prune proof"
      pattern: "npm run build"
---

<objective>
Delete all **55** Phase 4–cleared unused `web/public` assets (ASSET-02), preserving every inventory `keep` file — especially public hero PNG, `mockups.css`, and live logos.

Purpose: Drop ~32.6 MB (`delete_bytes: 34209521` from Phase 2 inventory) of statically proven-unused public weight so the production tree is lean before parity verification (Phase 6).
Output: 55 paths gone; keeps intact; one delete commit; `npm run build` green; `05-01-SUMMARY.md` with ASSET-02 + savings.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-prune-public-assets/05-CONTEXT.md
@.planning/phases/04-minimal-react-unlock/04-UNLOCK.md
@.planning/phases/04-minimal-react-unlock/04-RESCAN.json
@.planning/phases/02-asset-inventory/02-INVENTORY.json
@.planning/phases/03-safe-orphan-deletes/03-01-SUMMARY.md

## Locked decisions (NON-NEGOTIABLE)

- **D-01:** After deletes, prove with file/git checks **and** `npm run build` in `web/`.

## Claude's Discretion (apply as written — from CONTEXT)

- **Delete SoT:** Exact paths from `04-RESCAN.json` where `status == still_unreferenced` (must be **55** and match Phase 2 inventory `class == "delete"` set). **Abort** if sets mismatch.
- **Commit shape:** One coherent commit removing all 55 paths (tracked assets + any untracked `.DS_Store` if listed).
- **Hard stop:** Never delete inventory `class: keep` or any path not on the cleared delete list — especially `web/public/assets/hero/Hero-background.png`, `web/public/mockups.css`, keep logos.
- Empty directories left after deletes may be removed **only if empty**; do not remove directories that still contain keep files.

## Deferred (MUST NOT implement)

- Parity visual check → Phase 6
- Mockups archive / retirement → Phases 7–9
- Any React/HTML/CSS caller edits

## Exact denylist — NEVER delete (hard gate)

- Any path in `02-INVENTORY.json` with `class == "keep"` (82 paths)
- Explicitly: `web/public/assets/hero/Hero-background.png`
- Explicitly: `web/public/mockups.css`
- Explicitly keep logos: `web/public/assets/logo/logo-horizontal-color-transparent.png`, `logo-horizontal-reverse.svg`, `logo-isotype-app.png`, `logo-isotype.png`
- Note: `web/public/assets/logo/logo-horizontal-color.png` **is** on the delete list (different file from `…-transparent.png`)

## Exact allowlist — DELETE only these 55 (from 04-RESCAN still_unreferenced)

```
web/public/.DS_Store
web/public/assets/.DS_Store
web/public/assets/awards/laurel.svg
web/public/assets/awards/mist-bg.jpg
web/public/assets/blogs/mist-bg.png
web/public/assets/hero/hero-media.png
web/public/assets/hero/hero.mp4
web/public/assets/logo/logo-horizontal-color.png
web/public/assets/markets-reach/commodities.svg
web/public/assets/markets-reach/currencies.svg
web/public/assets/markets-reach/discord.svg
web/public/assets/markets-reach/mist-bg.jpg
web/public/assets/mrkt/mist-bg.jpg
web/public/assets/mrkt/product-collage.png
web/public/assets/mrkt/product-collage.webp
web/public/assets/mt5-sim/mist-bg.jpg
web/public/assets/mt5-sim/mt5Logo-transparent.png
web/public/assets/mt5-sim/mt5Logo.png
web/public/assets/mt5-sim/mt5_section.png
web/public/assets/mt5-sim/mt5_trading.png
web/public/assets/payments/bitcoin_icon.png
web/public/assets/payments/dm-payment-options-darkmode.png
web/public/assets/payments/dm-payment-options.png
web/public/assets/payments/ethereum_icon.png
web/public/assets/payments/funding.png
web/public/assets/payments/icons/crypto-pair.png
web/public/assets/payments/icons/googlepay.png
web/public/assets/payments/icons/mastercard-circles.png
web/public/assets/payments/icons/upi.png
web/public/assets/payments/mastercard-logo.png
web/public/assets/payments/mastercard.svg
web/public/assets/payments/mastercard_icon.png
web/public/assets/payments/pay-icons-01.png
web/public/assets/payments/pay-icons-darkmode-01.png
web/public/assets/payments/pay-icons-darkmode.png
web/public/assets/payments/pay-icons-new-01-00.png
web/public/assets/payments/pay-icons-new-01.png
web/public/assets/payments/pay-icons.png
web/public/assets/payments/same-day-withdrawls-03.png
web/public/assets/payments/visa-logo.png
web/public/assets/payments/visa.svg
web/public/assets/platforms/ctrader-icon.png
web/public/assets/platforms/ctrader-lockup.png
web/public/assets/platforms/ctrader-logo2.png
web/public/assets/platforms/download-ctrader-logo.png
web/public/assets/platforms/mt5-icon.png
web/public/assets/platforms/mt5-lockup.png
web/public/assets/platforms/platforms-mist-water.jpg
web/public/assets/trade-better/icons/bull-wire.svg
web/public/assets/trader-community/avatars/avatar-1.webp
web/public/assets/trader-community/avatars/avatar-2.webp
web/public/assets/trader-community/avatars/avatar-3.webp
web/public/assets/trader-community/avatars/avatar-4.webp
web/public/assets/trader-community/avatars/avatar-5.webp
web/public/assets/trader-community/avatars/avatar-6.webp
```

## Planner preflight (2026-09-05)

- `still_unreferenced` count = 55; inventory delete count = 55; set equality = exact match; 0 missing on disk.
- Expected savings: **34209521 bytes (~32.6 MB)** from `02-INVENTORY.json` `summary.delete_bytes`.
- `04-UNLOCK.md` status: `VERIFY_ONLY_CLEAR` — Phase 5 may delete all 55.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Load and validate the 55-path delete list</name>
  <files>.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt</files>
  <read_first>
.planning/phases/05-prune-public-assets/05-CONTEXT.md,
.planning/phases/04-minimal-react-unlock/04-UNLOCK.md,
.planning/phases/04-minimal-react-unlock/04-RESCAN.json,
.planning/phases/02-asset-inventory/02-INVENTORY.json
  </read_first>
  <action>
Load delete SoT from `04-RESCAN.json`: every `candidates[]` entry with `status == "still_unreferenced"`.

**Hard gates (abort entire phase if any fail — do not delete anything):**

1. Count of `still_unreferenced` paths **must equal 55**.
2. Count of `02-INVENTORY.json` `files[]` with `class == "delete"` **must equal 55**.
3. Set equality: `still_unreferenced` path set **==** inventory delete path set (symmetric difference empty). If any path differs, **ABORT** and write why to chat — do not create delete list or proceed.
4. Confirm `04-UNLOCK.md` still states Phase 5 clearance / `VERIFY_ONLY_CLEAR` / that Phase 5 may delete the 55 paths.
5. For each of the 55 paths: confirm the file exists on disk under the project root (planner preflight found 0 missing; re-check).
6. Confirm denylist paths exist and are **not** in the delete set:
   - `web/public/assets/hero/Hero-background.png`
   - `web/public/mockups.css`
   - All four keep logos under `web/public/assets/logo/` listed in context denylist
7. Confirm `logo-horizontal-color.png` (delete) ≠ `logo-horizontal-color-transparent.png` (keep) — both must remain correctly classified.

Write `.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt` as one path per line (exactly the 55 validated paths, sorted or in rescan order — either OK; no extras). Do **not** delete any files in this task.
  </action>
  <verify>
    <automated>python3 -c "
import json
from pathlib import Path
root = Path('.')
rescan = json.loads((root/'.planning/phases/04-minimal-react-unlock/04-RESCAN.json').read_text())
inv = json.loads((root/'.planning/phases/02-asset-inventory/02-INVENTORY.json').read_text())
still = {c['path'] for c in rescan['candidates'] if c['status']=='still_unreferenced'}
delete = {f['path'] for f in inv['files'] if f['class']=='delete'}
lst = [l.strip() for l in (root/'.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt').read_text().splitlines() if l.strip()]
assert len(still)==55 and len(delete)==55 and still==delete, (len(still), len(delete), still^delete)
assert set(lst)==still and len(lst)==55
assert all((root/p).exists() for p in lst)
keeps = ['web/public/assets/hero/Hero-background.png','web/public/mockups.css','web/public/assets/logo/logo-horizontal-color-transparent.png']
assert all((root/k).exists() for k in keeps)
assert not any(k in still for k in keeps)
print('OK validate', len(lst), 'paths')
"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt` exists with exactly 55 non-empty lines
    - `python3` gate above exits 0 (set equality + on-disk + denylist not in delete set)
    - No files under `web/public/` were deleted in this task (`git status --short web/public` shows no D/ deletions yet)
  </acceptance_criteria>
  <done>Validated 55-path allowlist written; SoT intersect inventory OK; denylist intact; zero deletes performed.</done>
</task>

<task type="auto">
  <name>Task 2: Delete allowlisted paths only and commit once</name>
  <files>web/public/.DS_Store, web/public/assets/.DS_Store, web/public/assets/awards/laurel.svg, web/public/assets/awards/mist-bg.jpg, web/public/assets/blogs/mist-bg.png, web/public/assets/hero/hero-media.png, web/public/assets/hero/hero.mp4, web/public/assets/logo/logo-horizontal-color.png, web/public/assets/markets-reach/, web/public/assets/mrkt/, web/public/assets/mt5-sim/, web/public/assets/payments/, web/public/assets/platforms/, web/public/assets/trade-better/icons/bull-wire.svg, web/public/assets/trader-community/avatars/</files>
  <read_first>
.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt,
.planning/phases/05-prune-public-assets/05-CONTEXT.md,
.planning/phases/02-asset-inventory/02-INVENTORY.json
  </read_first>
  <action>
**Pre-delete hard gates (re-run before any rm):**

1. Re-read `05-DELETE-LIST.txt` — must still be exactly 55 lines matching Task 1 validation.
2. For every path about to be removed: assert it is in the delete list AND inventory `class == "delete"` AND **not** inventory `class == "keep"`.
3. Assert denylist paths still exist: `web/public/assets/hero/Hero-background.png`, `web/public/mockups.css`, keep logos.

**Delete procedure (allowlist only):**

1. For each path in `05-DELETE-LIST.txt`:
   - If tracked: `git rm -f -- "PATH"` (or batch `git rm -f -- $(cat …)` after confirming the arg list is exactly the 55).
   - If untracked (e.g. `.DS_Store`): `rm -f -- "PATH"` then ensure absence is reflected (untracked removal needs no git stage; note in SUMMARY).
2. **Never** pass a keep path to `git rm` / `rm`. Prefer a small Python/bash loop that deletes **only** lines from `05-DELETE-LIST.txt`.
3. After file deletes: optionally remove **empty** directories left under `web/public/assets/` that contained only deleted files. **Do not** remove a directory if any keep file remains inside. Do **not** remove `web/public` or `web/public/assets`.
4. Stage only the deletions (and empty-dir removals if tracked). Do **not** stage unrelated dirty files.
5. Create **exactly one** commit for all 55 removals, message via HEREDOC:

```
chore(05-01): prune 55 unused web/public assets (ASSET-02)

Remove Phase 4 still_unreferenced inventory delete paths (~32.6 MB).
Keep hero PNG, mockups.css, and all inventory keep assets.
```

6. Post-commit proofs:
   - Every delete-list path: `test ! -e PATH`
   - Every inventory keep path (82): still exists — scripted check required
   - Denylist hero PNG + mockups.css + keep logos: still exist
   - `git show --name-only --pretty=format: HEAD` must not list any keep-class path
  </action>
  <verify>
    <automated>python3 -c "
import json, subprocess
from pathlib import Path
root = Path('.')
lst = [l.strip() for l in (root/'.planning/phases/05-prune-public-assets/05-DELETE-LIST.txt').read_text().splitlines() if l.strip()]
assert len(lst)==55
missing_gone = [p for p in lst if (root/p).exists()]
assert not missing_gone, missing_gone
inv = json.loads((root/'.planning/phases/02-asset-inventory/02-INVENTORY.json').read_text())
keeps = [f['path'] for f in inv['files'] if f['class']=='keep']
assert len(keeps)==82
missing_keep = [p for p in keeps if not (root/p).exists()]
assert not missing_keep, missing_keep[:10]
for k in ['web/public/assets/hero/Hero-background.png','web/public/mockups.css','web/public/assets/logo/logo-horizontal-color-transparent.png','web/public/assets/logo/logo-horizontal-reverse.svg','web/public/assets/logo/logo-isotype-app.png','web/public/assets/logo/logo-isotype.png']:
    assert (root/k).exists(), k
# commit touched only delete paths (plus maybe empty dirs — must not include keep files)
show = subprocess.check_output(['git','show','--name-only','--pretty=format:','HEAD'], text=True)
changed = {l.strip() for l in show.splitlines() if l.strip()}
keep_set = set(keeps)
overlap = changed & keep_set
assert not overlap, overlap
print('OK delete+keeps', 'changed', len(changed))
"</automated>
  </verify>
  <acceptance_criteria>
    - All 55 paths from `05-DELETE-LIST.txt` are absent from disk
    - All 82 inventory keep paths still exist on disk
    - `web/public/assets/hero/Hero-background.png` and `web/public/mockups.css` still exist
    - Exactly one new commit on HEAD whose file list does not include any inventory keep path
    - No React/HTML/CSS source under `web/src` was modified in that commit
  </acceptance_criteria>
  <done>55 unused public assets removed in one commit; all keep assets remain; denylist untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Build proof and write ASSET-02 SUMMARY</name>
  <files>.planning/phases/05-prune-public-assets/05-01-SUMMARY.md</files>
  <read_first>
.planning/phases/05-prune-public-assets/05-CONTEXT.md,
.planning/REQUIREMENTS.md,
.planning/phases/02-asset-inventory/02-INVENTORY.json,
$HOME/.cursor/get-shit-done/templates/summary.md
  </read_first>
  <action>
Per **D-01**, run production build proof then document:

1. From `web/`: `npm run build` — must exit 0. Capture exit code and a short success excerpt for SUMMARY.
2. Reconfirm keeps after build: hero PNG + `mockups.css` still on disk.
3. Write `.planning/phases/05-prune-public-assets/05-01-SUMMARY.md` using the GSD summary template, including at minimum:
   - `requirements-completed: [ASSET-02]`
   - Deleted count: **55**
   - Byte savings estimate: **34209521** bytes (**~32.6 MB**) from `02-INVENTORY.json` `summary.delete_bytes` (state this is inventory-estimated, not re-measured du unless you also measure)
   - Proofs: file absence of deletes, keep presence, single commit hash/subject, `npm run build` exit 0
   - Explicit statement that denylist paths were not deleted
   - Deferred: parity → Phase 6; mockups → 7–9
4. Optionally commit the SUMMARY with docs commit if `commit_docs` workflow applies: `docs(05-01): summarize public asset prune` — do **not** mix SUMMARY into the asset-delete commit (that commit already landed in Task 2).
5. Do **not** mark REQUIREMENTS.md checkboxes unless execute-phase workflow expects it; SUMMARY must still name ASSET-02 complete.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-prune-public-assets/05-01-SUMMARY.md && grep -q 'ASSET-02' .planning/phases/05-prune-public-assets/05-01-SUMMARY.md && grep -E -q '34209521|32\.6' .planning/phases/05-prune-public-assets/05-01-SUMMARY.md && test -f web/public/assets/hero/Hero-background.png && test -f web/public/mockups.css && cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `05-01-SUMMARY.md` exists and contains the string `ASSET-02`
    - SUMMARY mentions savings `34209521` and/or `32.6` MB
    - `npm run build` in `web/` exits 0
    - Hero PNG and `mockups.css` still present after build
  </acceptance_criteria>
  <done>Build green; SUMMARY proves ASSET-02 with 55 deletes and ~32.6 MB savings; keeps verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner/executor → filesystem (`web/public`) | Agent may delete files; wrong path = permanent loss of live marketing assets |
| Delete list → git commit | Staged paths must equal allowlist only; keep files must never enter the commit |
| Build proof → deploy readiness | Green build does not prove visual parity (Phase 6) but catches missing static imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Accidental deletion of inventory `keep` paths (hero PNG, mockups.css, logos, body media) | **mitigate** (HIGH) | Hard gates: Task 1 set-equality + denylist existence; Task 2 pre-delete assert path ∈ delete∧∉keep; post-delete script asserts all 82 keeps exist; abort on any mismatch before `git rm` |
| T-05-02 | Tampering | Deleting path not on `still_unreferenced` / inventing deletes | **mitigate** | SoT = `04-RESCAN.json` only; intersect with inventory delete; `05-DELETE-LIST.txt` must equal that set; loop deletes only list lines |
| T-05-03 | Tampering | Confusing `logo-horizontal-color.png` (delete) with `logo-horizontal-color-transparent.png` (keep) | **mitigate** | Explicit denylist names transparent + other keep logos; Task 1 asserts transparent not in delete set; Task 2 keep check includes all four logo keeps |
| T-05-04 | Denial of service | Removing non-empty asset dirs that still hold keeps | **mitigate** | Empty-dir cleanup only when directory has zero remaining files; never remove `web/public` or `web/public/assets` |
| T-05-05 | Information disclosure | N/A — public marketing binaries, no secrets in delete set | **accept** | Orphan assets are public static files; no credential risk |
| T-05-06 | Elevation / spoofing | N/A — local filesystem ops, no auth boundary | **accept** | Local repo cleanup only |
| T-05-07 | Repudiation | Unclear what was deleted | **mitigate** | Single coherent commit + SUMMARY with counts, commit hash, and byte estimate |
</threat_model>

<verification>
- Task 1 automated set-equality script exits 0 before any delete
- Task 2 automated keep/delete presence script exits 0 after commit
- Task 3 `npm run build` in `web/` exits 0
- Manual spot-check optional later in Phase 6 (parity) — not required here
</verification>

<success_criteria>
- ASSET-02 satisfied: only inventory-proven unused public assets deleted
- 55 paths gone; 82 keeps remain; denylist untouched
- One delete commit; D-01 build proof green
- `05-01-SUMMARY.md` documents ASSET-02 + ~32.6 MB (34209521 bytes) savings
</success_criteria>

<output>
After completion, create `.planning/phases/05-prune-public-assets/05-01-SUMMARY.md`
</output>
