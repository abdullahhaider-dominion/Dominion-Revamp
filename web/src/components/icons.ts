/**
 * Lucide React is part of the project stack.
 * Re-export common icons for future componentization without changing
 * the current 1:1 vanilla visual replica.
 */
export {
  Menu,
  X,
  ChevronDown,
  ArrowRight,
  Play,
  Pause,
  ExternalLink,
} from "lucide-react";
